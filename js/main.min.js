const navToggle=document.getElementById("navToggle");const navLinks=document.getElementById("navLinks");navToggle.addEventListener("click",()=>{const isOpen=navLinks.classList.toggle("open");navToggle.setAttribute("aria-expanded",isOpen)});navLinks.querySelectorAll("a").forEach(link=>{link.addEventListener("click",()=>{navLinks.classList.remove("open");navToggle.setAttribute("aria-expanded","false")})});document.getElementById("year").textContent=new Date().getFullYear();const gameThumbSvg=document.querySelector(".game-thumb-svg");if(gameThumbSvg&&window.matchMedia("(prefers-reduced-motion: reduce)").matches){gameThumbSvg.pauseAnimations()}(function initCustomCursor(){const supportsCursor=window.matchMedia("(pointer: fine)").matches&&!window.matchMedia("(prefers-reduced-motion: reduce)").matches;if(!supportsCursor||document.querySelector(".dev-tabs"))return;const dot=document.createElement("div");dot.className="custom-cursor-dot";document.body.append(dot);const hoverSelector="a, button, input, textarea, select, .service-row, .tier-card";document.addEventListener("mousemove",e=>{document.body.classList.add("custom-cursor-active");dot.classList.add("is-visible");dot.style.setProperty("--cx",`${e.clientX-4}px`);dot.style.setProperty("--cy",`${e.clientY-4}px`);dot.classList.toggle("is-hovering",!!e.target.closest?.(hoverSelector))});document.addEventListener("mouseleave",()=>{document.body.classList.remove("custom-cursor-active");dot.classList.remove("is-visible")})})();(function initMatrixTrail(){const reducedMotion=window.matchMedia("(prefers-reduced-motion: reduce)").matches;if(reducedMotion||window.matchMedia("(pointer: coarse)").matches||document.querySelector(".dev-tabs"))return;const canvas=document.createElement("canvas");canvas.className="matrix-trail-canvas";document.body.append(canvas);const ctx=canvas.getContext("2d");function resize(){canvas.width=window.innerWidth;canvas.height=window.innerHeight}resize();window.addEventListener("resize",resize);const GLYPHS=["0","1","0","1","0","1","{","}","<",">","/","01"];const COLORS=["109, 78, 232","46, 159, 230"];const MAX_PARTICLES=160;let particles=[];const supportsCursor=window.matchMedia("(pointer: fine)").matches;if(supportsCursor){let lastSpawnAt=0;let lastX=null;let lastY=null;document.addEventListener("mousemove",e=>{const now=performance.now();const moved=lastX===null||Math.hypot(e.clientX-lastX,e.clientY-lastY)>10;if(moved&&now-lastSpawnAt>45&&particles.length<MAX_PARTICLES){lastSpawnAt=now;lastX=e.clientX;lastY=e.clientY;particles.push({x:e.clientX+(Math.random()-.5)*14,y:e.clientY+(Math.random()-.5)*14,vy:18+Math.random()*22,char:GLYPHS[Math.floor(Math.random()*GLYPHS.length)],color:COLORS[Math.floor(Math.random()*COLORS.length)],life:0,maxLife:.7+Math.random()*.5,size:11+Math.random()*6})}});document.addEventListener("mouseleave",()=>{particles=particles.filter(p=>p.ambient)})}function attachCodeRain(el,intervalMs){const spawnDrip=()=>{if(particles.length>=MAX_PARTICLES)return;const rect=el.getBoundingClientRect();if(rect.bottom<0||rect.top>window.innerHeight)return;particles.push({x:rect.left+Math.random()*rect.width,y:rect.bottom-2,vy:24+Math.random()*22,char:GLYPHS[Math.floor(Math.random()*GLYPHS.length)],color:COLORS[Math.floor(Math.random()*COLORS.length)],life:0,maxLife:.4+Math.random()*.28,size:10+Math.random()*4,ambient:true})};setInterval(spawnDrip,intervalMs);setTimeout(spawnDrip,Math.random()*intervalMs)}document.querySelectorAll(".quick-link-tab, .service-row").forEach(el=>{attachCodeRain(el,1500+Math.random()*1100)});let lastT=null;function tick(t){if(lastT===null)lastT=t;const dt=Math.min((t-lastT)/1e3,.05);lastT=t;ctx.clearRect(0,0,canvas.width,canvas.height);particles=particles.filter(p=>p.life<p.maxLife);for(const p of particles){p.life+=dt;p.y+=p.vy*dt;const fade=1-p.life/p.maxLife;const alpha=fade*(p.ambient?.45:.85);ctx.font=`700 ${p.size}px "Space Grotesk", monospace`;ctx.fillStyle=`rgba(${p.color}, ${alpha})`;ctx.shadowColor=`rgba(${p.color}, ${alpha})`;ctx.shadowBlur=6;ctx.fillText(p.char,p.x,p.y)}requestAnimationFrame(tick)}requestAnimationFrame(tick)})();function isConstrainedConnection(){if(window.matchMedia("(max-width: 800px), (prefers-reduced-motion: reduce)").matches)return true;const conn=navigator.connection||navigator.mozConnection||navigator.webkitConnection;if(!conn)return false;if(conn.saveData)return true;if(conn.effectiveType&&["slow-2g","2g"].includes(conn.effectiveType))return true;return false}function loadBackgroundVideo(video){if(!video||video.dataset.loaded)return;video.dataset.loaded="true";const source=document.createElement("source");source.src=video.dataset.src;source.type="video/mp4";video.appendChild(source);video.load();video.play().catch(()=>{})}if(!isConstrainedConnection()){const heroBgVideo=document.getElementById("heroBgVideo");if(heroBgVideo)loadBackgroundVideo(heroBgVideo);const workBgVideo=document.getElementById("workBgVideo");if(workBgVideo&&"IntersectionObserver"in window){const workVideoObserver=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){loadBackgroundVideo(workBgVideo);workVideoObserver.disconnect()}})},{rootMargin:"300px"});workVideoObserver.observe(workBgVideo)}else if(workBgVideo){loadBackgroundVideo(workBgVideo)}}(function carryOverLegacyKeys(){const renamed=[["veloraCart","digicodeCart"],["veloraCustomerInfo","digicodeCustomerInfo"],["veloraPageSpeedKey","digicodePageSpeedKey"]];try{renamed.forEach(([from,to])=>{const value=localStorage.getItem(from);if(value===null)return;if(localStorage.getItem(to)===null)localStorage.setItem(to,value);localStorage.removeItem(from)})}catch(e){}})();const CART_KEY="digicodeCart";const CUSTOMER_KEY="digicodeCustomerInfo";function getCart(){try{return JSON.parse(localStorage.getItem(CART_KEY))||[]}catch{return[]}}function saveCartItems(items){localStorage.setItem(CART_KEY,JSON.stringify(items))}function addToCart(item){const cart=getCart();const existingIndex=cart.findIndex(c=>c.key===item.key);if(existingIndex>-1)cart[existingIndex]=item;else cart.push(item);saveCartItems(cart)}function removeFromCart(key){const cart=getCart().filter(c=>c.key!==key);saveCartItems(cart);return cart}function updateCartBadge(){const count=getCart().length;document.querySelectorAll(".cart-badge").forEach(badge=>{badge.textContent=count;badge.hidden=count===0})}updateCartBadge();let projectsCache=[];let teamCache=[];let chatsCache={};function mapProjectRow(row){return{id:row.id,status:row.status,service:row.service,buildTier:row.build_tier,managementTier:row.management_tier,clientName:row.client_name,clientEmail:row.client_email,clientDiscord:row.client_discord,clientMobile:row.client_mobile,details:row.details,createdAt:row.created_at,assignedDev:row.assigned_dev,assignedAt:row.assigned_at,progressNotes:row.progress_notes||[],finishedAt:row.finished_at,review:row.review,domain:row.domain,health:row.health,orderKind:row.order_kind||"enquiry",amountCents:row.amount_cents,cancelledAt:row.cancelled_at,cancelledReason:row.cancelled_reason}}async function loadProjects(){const{data,error}=await digicodeSupabase.from("projects").select("*").order("created_at",{ascending:false});if(!error)projectsCache=(data||[]).map(mapProjectRow);return projectsCache}function getProjects(){return projectsCache}async function pushPendingProjectFromOrder(cart,customer){if(!cart.length)return;const buildItem=cart.find(i=>i.type==="Build");const mgmtItem=cart.find(i=>i.type==="Management");const serviceName=customer?.service||cart[0].service||"Website";const getLine=label=>{const line=customer?.lines?.find(l=>l.startsWith(label+":"));return line?line.slice(label.length+1).trim():""};const existing=getLine("Existing website");const domain=!existing||/^not yet$/i.test(existing.trim())?null:/^https?:\/\//i.test(existing)?existing:"https://"+existing;await digicodeSupabase.from("projects").insert({status:"pending",service:serviceName,build_tier:buildItem?`${buildItem.name} (${buildItem.price})`:null,management_tier:mgmtItem?`${mgmtItem.name} (${mgmtItem.price}${mgmtItem.period?" "+mgmtItem.period:""})`:null,client_name:getLine("Name"),client_email:getLine("Email"),client_discord:getLine("Discord"),client_mobile:getLine("Mobile"),details:customer?.lines?.filter(l=>!/^Name:|^Email:|^Discord:|^Mobile:/.test(l)).join(" \xB7 ")||"No additional project details provided.",domain})}function mapDeveloperRow(row){return{id:String(row.id),name:row.name,email:row.email,username:row.username,rank:row.rank,permissions:row.permissions||[]}}async function loadTeam(){const{data,error}=await digicodeSupabase.from("developers").select("*").order("created_at",{ascending:true});if(!error)teamCache=(data||[]).map(mapDeveloperRow);return teamCache}function getTeam(){return teamCache}async function loadChats(){const{data,error}=await digicodeSupabase.from("chats").select("*").order("created_at",{ascending:true});if(error)return chatsCache;const grouped={};(data||[]).forEach(row=>{if(!grouped[row.chat_id])grouped[row.chat_id]=[];grouped[row.chat_id].push({from:row.from_name,text:row.message,at:row.created_at})});chatsCache=grouped;return chatsCache}function getChats(){return chatsCache}let visitorChatsCache=[];let visitorMsgsCache={};async function loadVisitorChats(){const[convos,msgs]=await Promise.all([digicodeSupabase.from("visitor_chats").select("*").order("last_message_at",{ascending:false}).limit(100),digicodeSupabase.from("visitor_messages").select("*").order("id",{ascending:true}).limit(2e3)]);if(!convos.error)visitorChatsCache=convos.data||[];if(!msgs.error){const grouped={};(msgs.data||[]).forEach(row=>{if(!grouped[row.conversation_id])grouped[row.conversation_id]=[];grouped[row.conversation_id].push(row)});visitorMsgsCache=grouped}return visitorChatsCache}function getVisitorChats(){return visitorChatsCache}function getVisitorMessages(id){return visitorMsgsCache[id]||[]}const VISITOR_SEEN_KEY="digicodeVisitorSeen";function getVisitorSeen(){try{return JSON.parse(localStorage.getItem(VISITOR_SEEN_KEY)||"{}")}catch(e){return{}}}function markVisitorSeen(convoId){const msgs=getVisitorMessages(convoId);const last=msgs[msgs.length-1];if(!last)return;const seen=getVisitorSeen();seen[convoId]=last.id;try{localStorage.setItem(VISITOR_SEEN_KEY,JSON.stringify(seen))}catch(e){}}function visitorUnreadCount(convoId){const seenId=getVisitorSeen()[convoId]||0;return getVisitorMessages(convoId).filter(m=>m.sender==="visitor"&&m.id>seenId).length}const PSI_KEY_STORAGE="digicodePageSpeedKey";function getPsiKey(){return localStorage.getItem(PSI_KEY_STORAGE)||""}function savePsiKey(key){localStorage.setItem(PSI_KEY_STORAGE,key)}function formatDate(ts){if(!ts)return"\u2014";const d=new Date(ts);return d.toLocaleDateString(void 0,{year:"numeric",month:"short",day:"numeric"})+" \xB7 "+d.toLocaleTimeString(void 0,{hour:"2-digit",minute:"2-digit"})}function formatDay(ts){if(!ts)return"-";return new Date(ts).toLocaleDateString(void 0,{year:"numeric",month:"short",day:"numeric"})}function escapeHtml(str){return String(str??"").replace(/[&<>"']/g,c=>({"&":"&amp;","<":"&lt;",">":"&gt;",'"':"&quot;","'":"&#39;"})[c])}const heroLogo=document.getElementById("heroLogo");const maxMove=22;const maxTilt=14;const isMobileViewport=()=>window.matchMedia("(max-width: 860px)").matches;if(heroLogo){heroLogo.addEventListener("mousemove",e=>{if(isMobileViewport())return;const rect=heroLogo.getBoundingClientRect();const relX=(e.clientX-rect.left)/rect.width-.5;const relY=(e.clientY-rect.top)/rect.height-.5;const moveX=relX*maxMove;const moveY=relY*maxMove;const tiltX=-relY*maxTilt;const tiltY=relX*maxTilt;heroLogo.style.transform=`translate(${moveX}px, ${moveY}px) rotateX(${tiltX}deg) rotateY(${tiltY}deg)`});heroLogo.addEventListener("mouseleave",()=>{heroLogo.style.transform="translate(0, 0) rotateX(0deg) rotateY(0deg)"})}let audioCtx;function getAudioCtx(){if(!audioCtx)audioCtx=new(window.AudioContext||window.webkitAudioContext);return audioCtx}["pointerdown","keydown","touchstart"].forEach(evt=>{document.addEventListener(evt,()=>{const ctx=getAudioCtx();if(ctx.state==="suspended")ctx.resume()},{once:true,passive:true})});function playHoverClick(){const ctx=getAudioCtx();if(ctx.state==="suspended")ctx.resume();const duration=.055;const bufferSize=Math.floor(ctx.sampleRate*duration);const buffer=ctx.createBuffer(1,bufferSize,ctx.sampleRate);const data=buffer.getChannelData(0);for(let i=0;i<bufferSize;i++){data[i]=Math.random()*2-1}const noise=ctx.createBufferSource();noise.buffer=buffer;const filter=ctx.createBiquadFilter();filter.type="bandpass";filter.frequency.value=2400;filter.Q.value=1.1;const gain=ctx.createGain();const now=ctx.currentTime;gain.gain.setValueAtTime(1e-4,now);gain.gain.exponentialRampToValueAtTime(.3,now+.004);gain.gain.exponentialRampToValueAtTime(1e-4,now+duration);noise.connect(filter).connect(gain).connect(ctx.destination);noise.start(now);noise.stop(now+duration)}document.querySelectorAll(".service-row").forEach(row=>{row.addEventListener("mouseenter",playHoverClick)});const requestForm=document.querySelector(".request-form");if(requestForm){let wireReveal2=function(radioName,triggerValue,targetId){const radios=requestForm.querySelectorAll(`input[name="${radioName}"]`);const target=document.getElementById(targetId);if(!radios.length||!target)return;radios.forEach(radio=>{radio.addEventListener("change",()=>{const match=requestForm.querySelector(`input[name="${radioName}"]:checked`)?.value===triggerValue;target.hidden=!match})})};var wireReveal=wireReveal2;wireReveal2("hasDomain","no","domainFollowup");wireReveal2("wantsSubscription","yes","tierGrid");requestForm.querySelectorAll(".tier-card").forEach(card=>{card.addEventListener("click",e=>{if(e.target.closest(".tier-purchase-btn"))return;const radio=card.querySelector(".tier-radio");if(radio)radio.checked=true})});requestForm.querySelectorAll(".tier-purchase-btn").forEach(btn=>{btn.addEventListener("click",e=>{e.stopPropagation();const card=btn.closest(".tier-card");const radio=card?.querySelector(".tier-radio");if(!radio)return;radio.checked=true;const serviceName=requestForm.dataset.service||"Website";const name=card.querySelector(".tier-name")?.textContent.trim()||radio.value;const price=card.querySelector(".price-amount")?.textContent.trim()||"";const period=card.querySelector(".price-period")?.textContent.trim()||"";addToCart({key:`${serviceName}::${radio.name}`,service:serviceName,type:radio.name==="buildTier"?"Build":"Management",name,price,period});updateCartBadge();const originalText=btn.textContent;btn.textContent="Added \u2713";btn.disabled=true;setTimeout(()=>{btn.textContent=originalText;btn.disabled=false},1200)})});const billingTabs=requestForm.querySelectorAll(".billing-tab");const billingCycleInput=document.getElementById("billingCycle");billingTabs.forEach(tab=>{tab.addEventListener("click",()=>{billingTabs.forEach(t=>t.classList.remove("active"));tab.classList.add("active");const cycle=tab.dataset.billing;if(billingCycleInput)billingCycleInput.value=cycle==="weekly"?"Weekly":"Monthly";const subscriptionPanel=document.getElementById("tierGrid");subscriptionPanel?.querySelectorAll(".price-amount").forEach(el=>{el.textContent=cycle==="weekly"?el.dataset.weekly:el.dataset.monthly});subscriptionPanel?.querySelectorAll(".price-period").forEach(el=>{el.textContent=cycle==="weekly"?"/wk":"/mo"})})});const quoteBox=document.getElementById("wantQuote");const quoteDetail=document.getElementById("quoteDetail");const wantsQuote=()=>!!(quoteBox&&quoteBox.checked);const checkoutBtn=document.getElementById("checkoutBtn");if(quoteBox){quoteBox.addEventListener("change",()=>{if(quoteDetail)quoteDetail.hidden=!quoteBox.checked;if(!checkoutBtn)return;checkoutBtn.textContent=quoteBox.checked?"Request a Quote":checkoutBtn.dataset.enquiry==="true"?"Send Request":"Checkout";if(quoteBox.checked){requestForm.querySelectorAll('input[name="buildTier"]').forEach(r=>r.checked=false)}})}if(checkoutBtn){checkoutBtn.addEventListener("click",()=>{const lines=[];requestForm.querySelectorAll("input, textarea").forEach(el=>{if(el.name==="buildTier"||el.name==="subscriptionTier")return;const label=el.dataset.label;if(!label)return;if(el.type==="radio"||el.type==="checkbox"){if(el.checked)lines.push(label)}else{const val=el.value.trim();if(val)lines.push(`${label}: ${val}`)}});const serviceName=requestForm.dataset.service||"Website";if(typeof window.digicodeUploads==="function"){lines.push(...window.digicodeUploads())}localStorage.setItem(CUSTOMER_KEY,JSON.stringify({service:serviceName,lines}));if(wantsQuote()){checkoutBtn.disabled=true;checkoutBtn.textContent="Sending\u2026";const field=label=>{const line=lines.find(l=>l.startsWith(label+":"));return line?line.slice(label.length+1).trim():""};digicodeSupabase.rpc("request_quote",{p_service:serviceName+" \u2014 quote",p_name:field("Name"),p_email:field("Email"),p_details:lines.join("\n")}).catch(()=>{}).then(()=>{const subject=encodeURIComponent(`Quote request \u2014 ${serviceName}`);const body=encodeURIComponent([`Quote request \u2014 ${serviceName}`,""].concat(lines).join("\n"));window.location.href=`mailto:developerteam@digi-code.com.au?subject=${subject}&body=${body}`;checkoutBtn.disabled=false;checkoutBtn.textContent="Request a Quote"});return}if(checkoutBtn.dataset.enquiry==="true"){const subject=encodeURIComponent(`${serviceName} enquiry`);const body=encodeURIComponent([`${serviceName} enquiry:`,""].concat(lines).join("\n"));window.location.href=`mailto:developerteam@digi-code.com.au?subject=${subject}&body=${body}`;return}window.location.href=checkoutBtn.dataset.cartUrl||"cart.html"})}}const cartItemsEl=document.getElementById("cartItems");if(cartItemsEl){let getDiscount2=function(){try{const raw=sessionStorage.getItem(DISCOUNT_KEY);if(!raw)return null;const d=JSON.parse(raw);if(d.expires_at&&new Date(d.expires_at)<=new Date){sessionStorage.removeItem(DISCOUNT_KEY);return null}return d}catch(e){return null}},setDiscount2=function(d){try{if(d)sessionStorage.setItem(DISCOUNT_KEY,JSON.stringify(d));else sessionStorage.removeItem(DISCOUNT_KEY)}catch(e){}},discountLabel2=function(d){const value=d.kind==="percent"?`${+d.amount}% off`:`$${(+d.amount).toFixed(2)} off`;const scope=d.applies_to==="one_time"?"one-time":d.applies_to==="subscription"?"subscription":"everything";return`${d.code.toUpperCase()} \u2014 ${value} ${scope}`},discountOn2=function(d,amount){if(!d||amount<=0)return 0;const off=d.kind==="percent"?amount*(+d.amount/100):+d.amount;return Math.min(off,amount)},renderCart2=function(){const cart=getCart();if(!cart.length){if(cartEmptyEl)cartEmptyEl.hidden=false;if(cartSummaryEl)cartSummaryEl.hidden=true;cartItemsEl.innerHTML="";return}if(cartEmptyEl)cartEmptyEl.hidden=true;if(cartSummaryEl)cartSummaryEl.hidden=false;cartItemsEl.innerHTML=cart.map(item=>`
      <div class="cart-item">
        <div class="cart-item-info">
          <span class="cart-item-service">${item.service} \u2014 ${item.type}</span>
          <span class="cart-item-name">${item.name}</span>
        </div>
        <div class="cart-item-price">${item.price}<span class="price-period">${item.period?" "+item.period:""}</span></div>
        <button type="button" class="cart-item-remove" data-key="${item.key}" aria-label="Remove ${item.name}">&times;</button>
      </div>`).join("");cartItemsEl.querySelectorAll(".cart-item-remove").forEach(btn=>{btn.addEventListener("click",()=>{removeFromCart(btn.dataset.key);updateCartBadge();renderCart2()})});let oneTimeTotal=0;let subLine="";cart.forEach(item=>{const num=parseFloat(String(item.price).replace(/[^0-9.]/g,""))||0;if(item.period==="one-time"){oneTimeTotal+=num}else{subLine=subLine?`${subLine}, ${item.price}${item.period}`:`${item.price}${item.period}`}});const oneTimeTotalEl=document.getElementById("oneTimeTotal");if(oneTimeTotalEl)oneTimeTotalEl.textContent="$"+oneTimeTotal.toFixed(0);const subRow=document.getElementById("subscriptionRow");const subTotalEl=document.getElementById("subscriptionTotal");if(subRow&&subTotalEl){subRow.hidden=!subLine;subTotalEl.textContent=subLine}renderDiscount2(oneTimeTotal,cart)},renderDiscount2=function(oneTimeTotal,cart){const d=getDiscount2();const appliedEl=document.getElementById("discountApplied");const tagEl=document.getElementById("discountTag");const rowEl=document.getElementById("discountedRow");const labelEl=document.getElementById("discountedLabel");const totalEl=document.getElementById("discountedTotal");const inputWrap=document.querySelector(".cart-discount-row");if(!appliedEl||!rowEl)return;if(!d){appliedEl.hidden=true;rowEl.hidden=true;if(inputWrap)inputWrap.hidden=false;return}appliedEl.hidden=false;tagEl.textContent=discountLabel2(d);if(inputWrap)inputWrap.hidden=true;const subTotal=cart.filter(i=>i.period!=="one-time").reduce((n,i)=>n+(parseFloat(String(i.price).replace(/[^0-9.]/g,""))||0),0);const offOneTime=d.applies_to==="subscription"?0:discountOn2(d,oneTimeTotal);const offSub=d.applies_to==="one_time"?0:discountOn2(d,subTotal);const parts=[];if(offOneTime>0)parts.push(`$${(oneTimeTotal-offOneTime).toFixed(2)} one-time`);if(offSub>0)parts.push(`$${(subTotal-offSub).toFixed(2)}/mo`);if(!parts.length){rowEl.hidden=true;showDiscountMsg2("That code doesn't apply to anything in your cart.",true);return}labelEl.textContent=`After discount (\u2212$${(offOneTime+offSub).toFixed(2)})`;totalEl.textContent=parts.join(" + ");rowEl.hidden=false},showDiscountMsg2=function(text,isError){const el=document.getElementById("discountMsg");if(!el)return;el.textContent=text;el.hidden=!text;el.classList.toggle("is-error",!!isError)},skuFor2=function(item){if(item.sku)return item.sku;const name=(item.name||"").toLowerCase();const weekly=/wk|week/.test(item.period||"");if(item.type==="Build"){if(name.includes("basic"))return"build-basic";if(name.includes("common"))return"build-common";if(name.includes("ultimate"))return"build-ultimate";return null}const suffix=weekly?"week":"month";if(name.includes("silver"))return`mgmt-silver-${suffix}`;if(name.includes("gold"))return`mgmt-gold-${suffix}`;if(name.includes("platinum"))return`mgmt-platinum-${suffix}`;return null},cartLine2=function(customer,label){const line=customer?.lines?.find(l=>l.startsWith(label+":"));return line?line.slice(label.length+1).trim():""},emailTheOrder2=function(cart,customer,discount){const lines=["Cart:"];cart.forEach(item=>{lines.push(`- ${item.service} \u2014 ${item.name} (${item.price}${item.period?" "+item.period:""})`)});if(discount){lines.push("");lines.push(`Discount code applied: ${discountLabel2(discount)}`)}if(customer?.lines?.length){lines.push("");lines.push(`Details (from ${customer.service}):`);customer.lines.forEach(l=>lines.push(l))}const subject=encodeURIComponent("New DigiCode Order");const body=encodeURIComponent(lines.join("\n"));window.location.href=`mailto:developerteam@digi-code.com.au?subject=${subject}&body=${body}`};var getDiscount=getDiscount2,setDiscount=setDiscount2,discountLabel=discountLabel2,discountOn=discountOn2,renderCart=renderCart2,renderDiscount=renderDiscount2,showDiscountMsg=showDiscountMsg2,skuFor=skuFor2,cartLine=cartLine2,emailTheOrder=emailTheOrder2;const cartEmptyEl=document.getElementById("cartEmpty");const cartSummaryEl=document.getElementById("cartSummary");const DISCOUNT_KEY="digicodeDiscount";const applyBtn=document.getElementById("applyDiscountBtn");const codeInput=document.getElementById("discountCodeInput");async function applyDiscountCode(){const raw=(codeInput.value||"").trim();if(!raw)return;applyBtn.disabled=true;showDiscountMsg2("Checking\u2026",false);try{const{data,error}=await digicodeSupabase.rpc("validate_discount_code",{p_code:raw});if(error)throw error;const row=Array.isArray(data)?data[0]:data;if(!row){setDiscount2(null);showDiscountMsg2("That code isn't valid or has expired.",true)}else{setDiscount2(row);showDiscountMsg2("",false);codeInput.value=""}}catch(e){showDiscountMsg2("Couldn't check that code. Try again in a moment.",true)}finally{applyBtn.disabled=false;renderCart2()}}applyBtn?.addEventListener("click",applyDiscountCode);codeInput?.addEventListener("keydown",e=>{if(e.key==="Enter"){e.preventDefault();applyDiscountCode()}});document.getElementById("removeDiscountBtn")?.addEventListener("click",()=>{setDiscount2(null);showDiscountMsg2("",false);renderCart2()});renderCart2();(async function handleCartReturn(){const params=new URLSearchParams(location.search);const ref=(params.get("ref")||"").trim();if(params.get("paid")!=="1"||!ref)return;const box=document.createElement("div");box.className="script-paid";box.innerHTML='<p class="dev-empty">Confirming your payment\u2026</p>';const anchor=cartEmptyEl||cartItemsEl;if(anchor&&anchor.parentNode)anchor.parentNode.insertBefore(box,anchor);let paid=false;try{const res=await fetch("https://phlmnlildwkkichlbtoy.supabase.co/functions/v1/confirm-order",{method:"POST",headers:{"Content-Type":"application/json",apikey:SUPABASE_ANON_KEY,Authorization:"Bearer "+SUPABASE_ANON_KEY},body:JSON.stringify({reference:ref})});const data=await res.json().catch(()=>({}));paid=!!data.paid}catch(e){}saveCartItems([]);setDiscount2(null);updateCartBadge();renderCart2();box.innerHTML=paid?`<h2>Thanks \u2014 payment received.</h2><p>Your order is in and our team can see it. We'll be in touch at the email address you gave Stripe. Keep this reference in case you need it.</p><p class="script-ref">Reference: `+escapeHtml(ref)+"</p>":`<h2>Thanks \u2014 payment received.</h2><p>We're still finalising your order. If you don't hear from us shortly, email <a href="mailto:developerteam@digi-code.com.au">developerteam@digi-code.com.au</a> with the reference below and we'll sort it out straight away.</p><p class="script-ref">Reference: `+escapeHtml(ref)+"</p>"})();const clearCartBtn=document.getElementById("clearCartBtn");clearCartBtn?.addEventListener("click",()=>{saveCartItems([]);updateCartBadge();renderCart2()});const CHECKOUT_FN="https://phlmnlildwkkichlbtoy.supabase.co/functions/v1/stripe-checkout";const confirmOrderBtn=document.getElementById("confirmOrderBtn");confirmOrderBtn?.addEventListener("click",async()=>{const cart=getCart();const customer=JSON.parse(localStorage.getItem(CUSTOMER_KEY)||"null");const discount=getDiscount2();if(!cart.length)return;const originalText=confirmOrderBtn.textContent;confirmOrderBtn.disabled=true;confirmOrderBtn.textContent="Taking you to checkout\u2026";const items=cart.map(i=>({sku:skuFor2(i),qty:1})).filter(i=>i.sku);if(items.length!==cart.length){confirmOrderBtn.disabled=false;confirmOrderBtn.textContent=originalText;await pushPendingProjectFromOrder(cart,customer);emailTheOrder2(cart,customer,discount);return}try{const res=await fetch(CHECKOUT_FN,{method:"POST",headers:{"Content-Type":"application/json",apikey:SUPABASE_ANON_KEY,Authorization:`Bearer ${SUPABASE_ANON_KEY}`},body:JSON.stringify({items,code:discount?discount.code:null,email:cartLine2(customer,"Email")||null,name:cartLine2(customer,"Name")||null})});const data=await res.json().catch(()=>({}));if(res.ok&&data.url){window.location.href=data.url;return}throw new Error(data.error||`checkout unavailable (${res.status})`)}catch(err){confirmOrderBtn.disabled=false;confirmOrderBtn.textContent=originalText;await pushPendingProjectFromOrder(cart,customer);if(discount){try{await digicodeSupabase.rpc("redeem_discount_code",{p_code:discount.code})}catch(e){}}emailTheOrder2(cart,customer,discount)}})}const portalLoginForm=document.getElementById("portalLoginForm");if(portalLoginForm){let showCodeMode2=function(show){portalLoginForm.hidden=show;if(passwordNote)passwordNote.hidden=show;if(codeForm){codeForm.hidden=!show;const idField=document.getElementById("codeIdentifier");if(idField)idField.required=show}if(backRow)backRow.hidden=!show};var showCodeMode=showCodeMode2;if(sessionStorage.getItem("digicodePortalDenied")){sessionStorage.removeItem("digicodePortalDenied");const deniedEl=document.getElementById("loginError");if(deniedEl){deniedEl.textContent="That account does not have access to the portal.";deniedEl.hidden=false}}if(sessionStorage.getItem("digicodePortalTimedOut")){sessionStorage.removeItem("digicodePortalTimedOut");const idleEl=document.getElementById("loginError");if(idleEl){idleEl.textContent="You were signed out after 15 minutes of inactivity. Sign in again to carry on.";idleEl.hidden=false}}const codeForm=document.getElementById("portalCodeForm");const backRow=document.getElementById("backToPasswordRow");const showCodeBtn=document.getElementById("showCodeFormBtn");const showPasswordBtn=document.getElementById("showPasswordFormBtn");const passwordNote=showCodeBtn?showCodeBtn.closest("p"):null;showCodeMode2(false);showCodeBtn?.addEventListener("click",()=>{const typed=document.getElementById("loginEmail")?.value.trim();const idField=document.getElementById("codeIdentifier");if(typed&&idField&&!idField.value)idField.value=typed;showCodeMode2(true)});showPasswordBtn?.addEventListener("click",()=>showCodeMode2(false));async function resolveIdentifier(identifier){const{data,error}=await digicodeSupabase.rpc("resolve_portal_login",{identifier});if(error)return{message:"We couldn't check that just now \u2014 please try again."};if(!data)return{message:"That email or username does not have access to the portal."};return{email:data}}portalLoginForm.addEventListener("submit",async e=>{e.preventDefault();const identifier=document.getElementById("loginEmail").value.trim();const password=document.getElementById("loginPassword").value;const errorEl=document.getElementById("loginError");const submitBtn=document.getElementById("loginSubmitBtn");if(errorEl)errorEl.hidden=true;if(submitBtn){submitBtn.disabled=true;submitBtn.textContent="Signing in\u2026"}const failWith=message=>{if(errorEl){errorEl.textContent=message;errorEl.hidden=false}if(submitBtn){submitBtn.disabled=false;submitBtn.textContent="Sign In"}};const resolved=await resolveIdentifier(identifier);if(!resolved.email){failWith(resolved.message);return}const{error}=await digicodeSupabase.auth.signInWithPassword({email:resolved.email,password});if(error){failWith("That password didn't work. If you haven't set one yet, get a one-time code below.");return}window.location.href="portal.html"});codeForm?.addEventListener("submit",async e=>{e.preventDefault();const identifier=document.getElementById("codeIdentifier").value.trim();const errorEl=document.getElementById("codeError");const submitBtn=document.getElementById("codeSubmitBtn");if(errorEl)errorEl.hidden=true;if(submitBtn){submitBtn.disabled=true;submitBtn.textContent="Sending\u2026"}const failWith=message=>{if(errorEl){errorEl.textContent=message;errorEl.hidden=false}if(submitBtn){submitBtn.disabled=false;submitBtn.textContent="Send Code"}};const resolved=await resolveIdentifier(identifier);if(!resolved.email){failWith(resolved.message);return}const{error}=await digicodeSupabase.auth.signInWithOtp({email:resolved.email,options:{shouldCreateUser:true}});if(error){failWith("Something went wrong sending your code \u2014 try again.");return}sessionStorage.setItem("digicodePendingEmail",resolved.email);window.location.href="verify.html"})}const verifyCodeForm=document.getElementById("verifyCodeForm");if(verifyCodeForm){const pendingEmail=sessionStorage.getItem("digicodePendingEmail");const subEl=document.getElementById("verifySub");if(pendingEmail&&subEl){subEl.textContent=`We've sent a one-time code to ${pendingEmail}. Enter it below and you'll choose your password next.`}else if(!pendingEmail){window.location.href="signin.html"}verifyCodeForm.addEventListener("submit",async e=>{e.preventDefault();const code=document.getElementById("oneTimeCode").value.trim();const errorEl=document.getElementById("verifyError");const submitBtn=document.getElementById("verifySubmitBtn");if(errorEl)errorEl.hidden=true;if(submitBtn){submitBtn.disabled=true;submitBtn.textContent="Verifying\u2026"}const{error}=await digicodeSupabase.auth.verifyOtp({email:pendingEmail,token:code,type:"email"});if(error){if(errorEl)errorEl.hidden=false;if(submitBtn){submitBtn.disabled=false;submitBtn.textContent="Verify Code"}return}sessionStorage.removeItem("digicodePendingEmail");const redirect=sessionStorage.getItem("digicodeAuthRedirect")||"create-password.html";sessionStorage.removeItem("digicodeAuthRedirect");window.location.href=redirect});document.getElementById("resendCodeBtn")?.addEventListener("click",async()=>{if(!pendingEmail)return;await digicodeSupabase.auth.signInWithOtp({email:pendingEmail,options:{shouldCreateUser:true}})})}const createPasswordForm=document.getElementById("createPasswordForm");if(createPasswordForm){const errorEl=document.getElementById("createPasswordError");const submitBtn=document.getElementById("createPasswordBtn");const showError=message=>{if(errorEl){errorEl.textContent=message;errorEl.hidden=false}};(async()=>{const{data:{session}}=await digicodeSupabase.auth.getSession();if(!session)window.location.href="signin.html"})();createPasswordForm.addEventListener("submit",async e=>{e.preventDefault();const password=document.getElementById("newPassword").value;const confirm=document.getElementById("confirmPassword").value;if(errorEl)errorEl.hidden=true;if(password.length<8){showError("Use at least 8 characters.");return}if(password!==confirm){showError("Those two passwords don't match.");return}if(submitBtn){submitBtn.disabled=true;submitBtn.textContent="Saving\u2026"}const{error}=await digicodeSupabase.auth.updateUser({password});if(error){showError(error.message||"We couldn't save that password \u2014 please try again.");if(submitBtn){submitBtn.disabled=false;submitBtn.textContent="Create Password"}return}window.location.href="portal.html"})}const resetPasswordForm=document.getElementById("resetPasswordForm");if(resetPasswordForm){resetPasswordForm.addEventListener("submit",async e=>{e.preventDefault();const newPassword=document.getElementById("newPassword").value;const confirmPassword=document.getElementById("confirmPassword").value;const errorEl=document.getElementById("resetError");const submitBtn=document.getElementById("resetSubmitBtn");if(errorEl)errorEl.hidden=true;if(newPassword!==confirmPassword){if(errorEl){errorEl.textContent="Those passwords don't match.";errorEl.hidden=false}return}if(submitBtn){submitBtn.disabled=true;submitBtn.textContent="Saving\u2026"}const{error}=await digicodeSupabase.auth.updateUser({password:newPassword});if(error){if(errorEl){errorEl.textContent="That link may have expired \u2014 request a new reset email from the game and try again.";errorEl.hidden=false}if(submitBtn){submitBtn.disabled=false;submitBtn.textContent="Set Password"}return}window.location.href="digicode-gaming.html"})}document.addEventListener("click",async e=>{if(!e.target.closest("[data-portal-logout]"))return;await digicodeSupabase.auth.signOut();window.location.href="signin.html"});const devTabs=document.querySelectorAll(".dev-tab");if(devTabs.length){let tierTagsHtml2=function(p){return`${p.buildTier?`<span class="project-tag">${p.buildTier}</span>`:""}${p.managementTier?`<span class="project-tag">${p.managementTier}</span>`:""}`},scoreClass2=function(score){if(score==null)return"";if(score>=90)return"health-good";if(score>=50)return"health-warning";return"health-critical"},domainAndHealthHtml2=function(p){const health=p.health;let healthHtml="";if(p.domain){if(health?.loading){healthHtml=`<div class="health-panel"><p class="health-loading">Running a live PageSpeed check on ${escapeHtml(p.domain)} \u2014 this takes 10\u201320 seconds\u2026</p></div>`}else if(health?.error){healthHtml=`
          <div class="health-panel">
            <p class="health-error">${escapeHtml(health.error)}</p>
            <button type="button" class="btn btn-ghost btn-small-inline check-health-btn" data-id="${p.id}">Try Again</button>
          </div>`}else if(health){healthHtml=`
          <div class="health-panel">
            <div class="health-header">
              <span>Website Health</span>
              <span class="health-meta">Checked ${formatDate(health.checkedAt)} \xB7 Live via Google PageSpeed Insights</span>
            </div>
            <div class="health-grid">
              <div class="stat-tile health-tile"><span class="stat-tile-label">Performance</span><span class="stat-tile-value ${scoreClass2(health.performance)}">${health.performance??"\u2014"}</span></div>
              <div class="stat-tile health-tile"><span class="stat-tile-label">SEO</span><span class="stat-tile-value ${scoreClass2(health.seo)}">${health.seo??"\u2014"}</span></div>
              <div class="stat-tile health-tile"><span class="stat-tile-label">Accessibility</span><span class="stat-tile-value ${scoreClass2(health.accessibility)}">${health.accessibility??"\u2014"}</span></div>
              <div class="stat-tile health-tile"><span class="stat-tile-label">Best Practices</span><span class="stat-tile-value ${scoreClass2(health.bestPractices)}">${health.bestPractices??"\u2014"}</span></div>
            </div>
            <p class="health-vitals">LCP ${escapeHtml(health.lcp)} \xB7 CLS ${escapeHtml(health.cls)} \xB7 FCP ${escapeHtml(health.fcp)}</p>
            <button type="button" class="btn btn-ghost btn-small-inline check-health-btn" data-id="${p.id}">Re-check</button>
          </div>`}else{healthHtml=`
          <div class="health-panel">
            <button type="button" class="btn btn-primary btn-small-inline check-health-btn" data-id="${p.id}">Check Website Health (Live)</button>
          </div>`}}return`
      <div class="domain-row">
        ${p.domain?`<span class="domain-value">${escapeHtml(p.domain)}</span> <button type="button" class="clear-cart-link edit-domain-btn" data-id="${p.id}">Unlink</button>`:`<form class="domain-form" data-id="${p.id}">
                <input type="text" class="domain-input" placeholder="clientwebsite.com" />
                <button type="submit" class="btn btn-ghost btn-small-inline">Link Domain</button>
              </form>`}
      </div>
      ${healthHtml}`},orderKindBadge2=function(p){if(p.orderKind==="paid"){const amount=p.amountCents?" \xB7 $"+(p.amountCents/100).toFixed(2):"";return`<div class="order-flag flag-paid">
                <svg viewBox="0 0 24 24" fill="none" width="13" height="13" aria-hidden="true"><path d="M5 12l4 4L19 6" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round"/></svg>
                Paid${escapeHtml(amount)} \u2014 ready to start
              </div>`}if(p.orderKind==="quote"){return`<div class="order-flag flag-quote">
                <svg viewBox="0 0 24 24" fill="none" width="13" height="13" aria-hidden="true"><path d="M12 17h.01M12 13a2.5 2.5 0 10-2.5-2.5" stroke="currentColor" stroke-width="2.2" stroke-linecap="round"/></svg>
                Quote requested \u2014 needs pricing
              </div>`}return`<div class="order-flag flag-enquiry">Enquiry \u2014 no payment taken</div>`},canRemoveProjects2=function(){const email=(window.digicodePortalEmail||"").toLowerCase();const me=getTeam().find(d=>(d.email||"").toLowerCase()===email);if(!me)return true;return me.rank==="Lead Developer"||(me.permissions||[]).includes("Delete Projects")},projectActionsHtml2=function(p){const del=canRemoveProjects2()?`<button type="button" class="clear-cart-link danger-link delete-project-btn" data-id="${p.id}">Delete</button>`:"";return`<div class="project-actions">
              <button type="button" class="clear-cart-link cancel-project-btn" data-id="${p.id}">Cancel</button>
              ${del}
            </div>`},wireDomainAndHealthHandlers2=function(panel){panel.querySelectorAll(".domain-form").forEach(form=>{form.addEventListener("submit",async e=>{e.preventDefault();const input=form.querySelector(".domain-input");const val=input.value.trim();if(!val)return;const domain=/^https?:\/\//i.test(val)?val:"https://"+val;await updateProjectFields(form.dataset.id,{domain,health:null});renderAllPanels2()})});panel.querySelectorAll(".edit-domain-btn").forEach(btn=>{btn.addEventListener("click",async()=>{await updateProjectFields(btn.dataset.id,{domain:null,health:null});renderAllPanels2()})});panel.querySelectorAll(".check-health-btn").forEach(btn=>{btn.addEventListener("click",()=>checkWebsiteHealth(btn.dataset.id))})},renderPendingPanel2=function(){const panel=document.getElementById("pendingPanel");if(!panel)return;const list=getProjects().filter(p=>p.status==="pending");const cancelled=getProjects().filter(p=>p.status==="cancelled");const cancelledHtml=cancelled.length?`<div class="cancelled-block">
           <h3 class="cancelled-head">Cancelled (${cancelled.length})</h3>
           ${cancelled.map(p=>`
             <div class="project-card is-cancelled">
               <div class="project-card-head">
                 <div>
                   <span class="project-service">${escapeHtml(p.service)}</span>
                   <span class="project-client">${escapeHtml(p.clientName)||"No name provided"}</span>
                 </div>
                 <span class="project-date">Cancelled ${formatDate(p.cancelledAt)}</span>
               </div>
               ${p.cancelledReason?`<p class="project-details">Reason: ${escapeHtml(p.cancelledReason)}</p>`:""}
               <div class="project-actions">
                 <button type="button" class="clear-cart-link restore-project-btn" data-id="${p.id}">Restore</button>
                 ${canRemoveProjects2()?`<button type="button" class="clear-cart-link danger-link delete-project-btn" data-id="${p.id}">Delete</button>`:""}
               </div>
             </div>`).join("")}
         </div>`:"";if(!list.length){panel.innerHTML=`<p class="dev-empty">No pending projects right now.</p>`+cancelledHtml;wireProjectActions2(panel);return}panel.innerHTML=list.map(p=>`
      <div class="project-card${p.orderKind==="quote"?" is-quote":p.orderKind==="paid"?" is-paid":""}">
        <div class="project-card-head">
          <div>
            <span class="project-service">${escapeHtml(p.service)}${p.sample?" \xB7 Sample":""}</span>
            <span class="project-client">${escapeHtml(p.clientName)||"No name provided"}</span>
          </div>
          <span class="project-date">Submitted ${formatDate(p.createdAt)}</span>
        </div>
        ${orderKindBadge2(p)}
        <div class="project-tiers">${tierTagsHtml2(p)}</div>
        <dl class="project-contact">
          <div><dt>Email</dt><dd>${escapeHtml(p.clientEmail)||"\u2014"}</dd></div>
          <div><dt>Discord</dt><dd>${escapeHtml(p.clientDiscord)||"\u2014"}</dd></div>
          <div><dt>Mobile</dt><dd>${escapeHtml(p.clientMobile)||"\u2014"}</dd></div>
        </dl>
        <p class="project-details">${escapeHtml(p.details)||"No project details provided."}</p>
        ${domainAndHealthHtml2(p)}
        <button type="button" class="btn btn-primary assign-btn" data-id="${p.id}">Assign to Me</button>
        ${projectActionsHtml2(p)}
      </div>`).join("")+cancelledHtml;panel.querySelectorAll(".assign-btn").forEach(btn=>{btn.addEventListener("click",()=>assignProject(btn.dataset.id))});wireProjectActions2(panel);wireDomainAndHealthHandlers2(panel)},wireProjectActions2=function(panel){panel.querySelectorAll(".cancel-project-btn").forEach(btn=>{btn.addEventListener("click",()=>cancelProject(btn.dataset.id))});panel.querySelectorAll(".delete-project-btn").forEach(btn=>{const p=getProjects().find(x=>x.id===btn.dataset.id);btn.addEventListener("click",()=>deleteProject(btn.dataset.id,p&&p.service||"this project",p&&p.orderKind))});panel.querySelectorAll(".restore-project-btn").forEach(btn=>{btn.addEventListener("click",()=>restoreProject(btn.dataset.id))})},renderAssignedPanel2=function(){const panel=document.getElementById("assignedPanel");if(!panel)return;const list=getProjects().filter(p=>p.status==="assigned");if(!list.length){panel.innerHTML=`<p class="dev-empty">No assigned projects right now.</p>`;return}panel.innerHTML=list.map(p=>`
      <div class="project-card">
        <div class="project-card-head">
          <div>
            <span class="project-service">${escapeHtml(p.service)}${p.sample?" \xB7 Sample":""}</span>
            <span class="project-client">${escapeHtml(p.clientName)||"No name provided"}</span>
          </div>
          <span class="project-date">Assigned ${formatDate(p.assignedAt)}</span>
        </div>
        <div class="project-tiers">${tierTagsHtml2(p)}</div>
        <p class="project-assigned-to">Assigned to <strong>${escapeHtml(p.assignedDev)}</strong></p>
        <div class="project-notes">
          ${p.progressNotes.length?p.progressNotes.map(n=>`<div class="project-note"><span class="project-note-date">${formatDate(n.date)}</span><p>${escapeHtml(n.note)}</p></div>`).join(""):`<p class="dev-empty">No progress notes yet.</p>`}
        </div>
        <form class="add-note-form" data-id="${p.id}">
          <input type="text" placeholder="Add a progress note\u2026" class="note-input" />
          <button type="submit" class="btn btn-ghost btn-small-inline">Add Note</button>
        </form>
        ${domainAndHealthHtml2(p)}
        <button type="button" class="btn btn-primary finish-btn" data-id="${p.id}">Mark as Finished</button>
      </div>`).join("");panel.querySelectorAll(".add-note-form").forEach(form=>{form.addEventListener("submit",e=>{e.preventDefault();const input=form.querySelector(".note-input");const text=input.value.trim();if(!text)return;addProgressNote(form.dataset.id,text)})});panel.querySelectorAll(".finish-btn").forEach(btn=>{btn.addEventListener("click",()=>finishProject(btn.dataset.id))});wireDomainAndHealthHandlers2(panel)},renderFinishedPanel2=function(){const panel=document.getElementById("finishedPanel");if(!panel)return;const list=getProjects().filter(p=>p.status==="finished");if(!list.length){panel.innerHTML=`<p class="dev-empty">No finished projects yet.</p>`;return}panel.innerHTML=list.map(p=>`
      <div class="project-card">
        <div class="project-card-head">
          <div>
            <span class="project-service">${escapeHtml(p.service)}${p.sample?" \xB7 Sample":""}</span>
            <span class="project-client">${escapeHtml(p.clientName)||"No name provided"}</span>
          </div>
          <span class="project-date">Finished ${formatDate(p.finishedAt)}</span>
        </div>
        <div class="project-tiers">${tierTagsHtml2(p)}</div>
        ${p.review?`<div class="project-review">
                <span class="project-review-stars">${"\u2605".repeat(p.review.rating)}${"\u2606".repeat(5-p.review.rating)}</span>
                <p>"${escapeHtml(p.review.text)}"</p>
              </div>`:`<p class="dev-empty">No customer review submitted yet.</p>`}
        ${domainAndHealthHtml2(p)}
      </div>`).join("");wireDomainAndHealthHandlers2(panel)},permissionCheckboxes2=function(granted){return DEV_PERMISSIONS.map(([value,label])=>`<label class="checkbox-option"><input type="checkbox" value="${value}"${granted.includes(value)?" checked":""} /> ${label}</label>`).join("")},canManageDevelopers2=function(){const email=(window.digicodePortalEmail||"").toLowerCase();const me=getTeam().find(d=>(d.email||"").toLowerCase()===email);if(!me)return true;return me.rank==="Lead Developer"||(me.permissions||[]).includes("Manage Developers")},renderDevelopersPanel2=function(){const panel=document.getElementById("developersPanel");if(!panel)return;const team=getTeam();const canManage=canManageDevelopers2();panel.innerHTML=`
      <div class="add-dev-form api-key-card">
        <h3 class="dashboard-section-title" style="margin-bottom: 8px; font-size: 1rem;">Website Health \u2014 API Settings</h3>
        <p class="form-hint">Live SEO/performance checks (in the project tabs) run on Google's free PageSpeed Insights API. Get a free key from Google Cloud Console (enable "PageSpeed Insights API," create an API key) and paste it here \u2014 it's stored only in this browser, never sent anywhere except directly to Google when you run a check.</p>
        <form id="psiKeyForm" class="form-grid" style="margin-top: 12px;">
          <div class="form-group" style="grid-column: 1 / -1;">
            <label for="psiKeyInput">Google PageSpeed API Key</label>
            <input type="text" id="psiKeyInput" placeholder="Paste your API key" value="${escapeHtml(getPsiKey())}" />
          </div>
        </form>
        <button type="button" id="savePsiKeyBtn" class="btn btn-ghost btn-small-inline">Save Key</button>
        <span id="psiKeySavedNote" class="dev-team-meta" style="margin-left: 10px;" hidden>Saved.</span>
      </div>

      ${canManage?`
      <div class="dev-warning-banner">Adding someone here grants real access \u2014 the moment they sign in at the normal sign-in page with this exact email, they'll get a real Dev account with this rank and these permissions. No password to set or share; they use the same one-time-code sign-in as everyone else.</div>
      <form id="addDevForm" class="add-dev-form">
        <div class="form-grid">
          <div class="form-group">
            <label for="newDevName">Name</label>
            <input type="text" id="newDevName" required />
          </div>
          <div class="form-group">
            <label for="newDevEmail">Email</label>
            <input type="email" id="newDevEmail" required />
          </div>
          <div class="form-group">
            <label for="newDevUsername">Username</label>
            <input type="text" id="newDevUsername" required />
          </div>
        </div>
        <div class="form-group">
          <label for="newDevRank">Rank</label>
          <select id="newDevRank">
            <option value="Junior Developer">Junior Developer</option>
            <option value="Developer" selected>Developer</option>
            <option value="Senior Developer">Senior Developer</option>
            <option value="Lead Developer">Lead Developer</option>
          </select>
        </div>
        <div class="form-group">
          <span class="form-label">Permissions</span>
          <div class="permission-grid">${permissionCheckboxes2(DEFAULT_NEW_DEV_PERMISSIONS)}</div>
        </div>
        <button type="submit" class="btn btn-primary">Create Developer</button>
      </form>
`:`<p class="dev-empty">Only a Lead Developer, or someone with the "Manage Developers" permission, can add or change accounts here.</p>`}

      <div class="dev-team-list">
        ${team.length?team.map(d=>`
          <div class="dev-team-row${editingDevId===d.id?" is-editing":""}">
            <div class="dev-team-info">
              <span class="dev-team-name"><span class="cosmic-name">${escapeHtml(d.name)}</span> <span class="rank-badge">${escapeHtml(d.rank)}</span></span>
              <span class="dev-team-meta">${escapeHtml(d.username)} \xB7 ${escapeHtml(d.email)}</span>
              <span class="dev-team-perms">${d.permissions.length?escapeHtml(d.permissions.join(", ")):"No permissions granted"}</span>
            </div>
            ${canManage?`<div class="dev-team-actions">
                     <button type="button" class="btn btn-ghost btn-small edit-dev-btn" data-id="${d.id}">${editingDevId===d.id?"Close":"Edit"}</button>
                     <button type="button" class="cart-item-remove remove-dev-btn" data-id="${d.id}" aria-label="Remove ${escapeHtml(d.name)}">&times;</button>
                   </div>`:""}
          </div>
          ${editingDevId===d.id&&canManage?`
          <form class="dev-edit-form" data-id="${d.id}">
            <h4>Editing ${escapeHtml(d.name)}</h4>
            <div class="form-group">
              <label for="editRank-${d.id}">Rank</label>
              <select id="editRank-${d.id}" class="edit-rank">
                ${DEV_RANKS.map(r=>`<option value="${r}"${d.rank===r?" selected":""}>${r}</option>`).join("")}
              </select>
              <span class="form-hint">Lead Developer carries every permission automatically, whatever is ticked below.</span>
            </div>
            <div class="form-group">
              <span class="form-label">Permissions</span>
              <div class="permission-grid edit-perms">${permissionCheckboxes2(d.permissions||[])}</div>
            </div>
            <p class="portal-login-error edit-dev-error" hidden></p>
            <div class="dev-edit-actions">
              <button type="submit" class="btn btn-primary btn-small">Save Changes</button>
              <button type="button" class="btn btn-ghost btn-small cancel-edit-btn">Cancel</button>
              <span class="dev-team-meta edit-dev-saved" hidden>Saved.</span>
            </div>
          </form>`:""}`).join(""):`<p class="dev-empty">No developers added yet.</p>`}
      </div>
    `;document.getElementById("savePsiKeyBtn")?.addEventListener("click",()=>{const val=document.getElementById("psiKeyInput").value.trim();savePsiKey(val);const note=document.getElementById("psiKeySavedNote");if(note){note.hidden=false;setTimeout(()=>note.hidden=true,2e3)}});document.getElementById("addDevForm")?.addEventListener("submit",async e=>{e.preventDefault();const name=document.getElementById("newDevName").value.trim();const email=document.getElementById("newDevEmail").value.trim();const username=document.getElementById("newDevUsername").value.trim();const rank=document.getElementById("newDevRank").value;const permissions=Array.from(panel.querySelectorAll(".permission-grid input:checked")).map(cb=>cb.value);await digicodeSupabase.from("developers").insert({name,email,username,rank,permissions});await loadTeam();renderDevelopersPanel2();renderChatPanel2()});panel.querySelectorAll(".remove-dev-btn").forEach(btn=>{btn.addEventListener("click",async()=>{const dev=getTeam().find(d=>String(d.id)===String(btn.dataset.id));if(!window.confirm(`Remove ${dev?dev.name:"this developer"} from the roster? They'll lose portal access.`))return;const{error}=await digicodeSupabase.from("developers").delete().eq("id",btn.dataset.id);if(error){window.alert("Couldn't remove them \u2014 your account may not have permission.");return}if(editingDevId===btn.dataset.id)editingDevId=null;await loadTeam();renderDevelopersPanel2()})});panel.querySelectorAll(".edit-dev-btn").forEach(btn=>{btn.addEventListener("click",()=>{const id=btn.dataset.id;editingDevId=editingDevId===id?null:id;renderDevelopersPanel2()})});panel.querySelectorAll(".cancel-edit-btn").forEach(btn=>{btn.addEventListener("click",()=>{editingDevId=null;renderDevelopersPanel2()})});panel.querySelectorAll(".dev-edit-form").forEach(form=>{form.addEventListener("submit",async e=>{e.preventDefault();const id=form.dataset.id;const err=form.querySelector(".edit-dev-error");const rank=form.querySelector(".edit-rank").value;const permissions=Array.from(form.querySelectorAll(".edit-perms input:checked")).map(cb=>cb.value);err.hidden=true;const{error}=await digicodeSupabase.from("developers").update({rank,permissions}).eq("id",id);if(error){err.textContent="Couldn't save that \u2014 your account may not have permission to change the roster.";err.hidden=false;return}await loadTeam();editingDevId=null;renderDevelopersPanel2();renderChatPanel2()})})},chatDisplayName2=function(chatId){if(chatId.startsWith("client:")){const project=getProjects().find(p=>p.id===chatId.slice(7));return project?project.clientName||"Unnamed client":"Unknown client"}if(chatId.startsWith("dev:")){const dev=getTeam().find(d=>d.id===chatId.slice(4));return dev?dev.name:"Unknown developer"}return"Unknown"},renderChatPanel2=function(){const panel=document.getElementById("chatPanel");if(!panel)return;const projects=getProjects().filter(p=>p.clientName);const team=getTeam();const chats=getChats();if(activeChatId){const stillExists=activeChatId.startsWith("client:")&&projects.some(p=>"client:"+p.id===activeChatId)||activeChatId.startsWith("dev:")&&team.some(d=>"dev:"+d.id===activeChatId)||activeChatId.startsWith("visitor:")&&getVisitorChats().some(c=>"visitor:"+c.id===activeChatId);if(!stillExists)activeChatId=null}if(chatListMode==="visitors"){renderVisitorChat2(panel);return}const lastMessagePreview=chatId=>{const msgs=chats[chatId]||[];const last=msgs[msgs.length-1];return last?escapeHtml(last.text):"No messages yet"};const listItemsHtml=chatListMode==="clients"?projects.length?projects.map(p=>{const chatId="client:"+p.id;return`
              <button type="button" class="chat-list-item ${activeChatId===chatId?"active":""}" data-chat-id="${chatId}">
                <span class="chat-list-name">${escapeHtml(p.clientName)}</span>
                <span class="chat-list-preview">${lastMessagePreview(chatId)}</span>
              </button>`}).join(""):`<p class="dev-empty">No client conversations yet.</p>`:team.length?team.map(d=>{const chatId="dev:"+d.id;return`
              <button type="button" class="chat-list-item ${activeChatId===chatId?"active":""}" data-chat-id="${chatId}">
                <span class="chat-list-name"><span class="cosmic-name">${escapeHtml(d.name)}</span> <span class="rank-badge">${escapeHtml(d.rank)}</span></span>
                <span class="chat-list-preview">${lastMessagePreview(chatId)}</span>
              </button>`}).join(""):`<p class="dev-empty">No developers added yet. Add one in the Developers tab.</p>`;const activeMsgs=activeChatId?chats[activeChatId]||[]:[];const threadHtml=activeChatId?`
        <div class="chat-thread-header">${escapeHtml(chatDisplayName2(activeChatId))}</div>
        <div class="chat-messages" id="chatMessages">
          ${activeMsgs.length?activeMsgs.map(m=>`
              <div class="chat-message ${m.from===window.digicodePortalEmail?"mine":"theirs"}">
                <div class="chat-message-bubble">${escapeHtml(m.text)}</div>
                <span class="chat-message-meta">${escapeHtml(m.from)} \xB7 ${formatDate(m.at)}</span>
              </div>`).join(""):`<p class="dev-empty">No messages yet \u2014 say hello.</p>`}
        </div>
        <form id="chatSendForm" class="chat-input-row">
          <input type="text" id="chatInput" class="chat-input" placeholder="Type a message\u2026" autocomplete="off" />
          <button type="submit" class="btn btn-primary">Send</button>
        </form>`:`<div class="chat-empty-state">Select a conversation to start chatting.</div>`;panel.innerHTML=`
      <div class="chat-layout">
        <div class="chat-sidebar">
          <div class="billing-toggle chat-mode-toggle">
            <button type="button" class="billing-tab chat-mode-btn ${chatListMode==="clients"?"active":""}" data-mode="clients">Clients</button>
            <button type="button" class="billing-tab chat-mode-btn ${chatListMode==="team"?"active":""}" data-mode="team">Talk to a Developer</button>
            <button type="button" class="billing-tab chat-mode-btn ${chatListMode==="visitors"?"active":""}" data-mode="visitors">Site Visitors${totalVisitorUnread2()?` (${totalVisitorUnread2()})`:""}</button>
          </div>
          <div class="chat-list">${listItemsHtml}</div>
        </div>
        <div class="chat-thread">${threadHtml}</div>
      </div>
    `;panel.querySelectorAll(".chat-mode-btn").forEach(btn=>{btn.addEventListener("click",()=>{chatListMode=btn.dataset.mode;activeChatId=null;renderChatPanel2()})});panel.querySelectorAll(".chat-list-item").forEach(btn=>{btn.addEventListener("click",()=>{activeChatId=btn.dataset.chatId;renderChatPanel2();const msgsEl=document.getElementById("chatMessages");if(msgsEl)msgsEl.scrollTop=msgsEl.scrollHeight})});const chatSendForm=document.getElementById("chatSendForm");chatSendForm?.addEventListener("submit",async e=>{e.preventDefault();const input=document.getElementById("chatInput");const text=input.value.trim();if(!text||!activeChatId)return;await digicodeSupabase.from("chats").insert({chat_id:activeChatId,from_name:window.digicodePortalEmail||"Dev",message:text});await loadChats();renderChatPanel2();const msgsEl=document.getElementById("chatMessages");if(msgsEl)msgsEl.scrollTop=msgsEl.scrollHeight})},totalVisitorUnread2=function(){return getVisitorChats().reduce((n,c)=>n+visitorUnreadCount(c.id),0)},canDeleteVisitorChats2=function(){const email=(window.digicodePortalEmail||"").toLowerCase();const me=getTeam().find(d=>(d.email||"").toLowerCase()===email);if(!me)return true;return me.rank==="Lead Developer"||(me.permissions||[]).includes("Delete Live Chats")},renderVisitorChat2=function(panel){const convos=getVisitorChats();const canDelete=canDeleteVisitorChats2();const activeId=activeChatId&&activeChatId.startsWith("visitor:")?Number(activeChatId.slice(8)):null;const listHtml=convos.length?convos.map(c=>{const msgs2=getVisitorMessages(c.id);const last=msgs2[msgs2.length-1];const unread=visitorUnreadCount(c.id);const chatId="visitor:"+c.id;const label=c.visitor_name||"Anonymous visitor";return`
              <div class="chat-visitor-row">
                <button type="button" class="chat-list-item ${activeChatId===chatId?"active":""}" data-chat-id="${chatId}">
                  <span class="chat-list-name">
                    ${escapeHtml(label)}
                    ${unread?`<span class="chat-visitor-badge">${unread} new</span>`:""}
                  </span>
                  <span class="chat-list-preview">${last?escapeHtml(last.body):"No messages yet"}</span>
                  <span class="chat-visitor-meta">${escapeHtml(c.visitor_email||"No email given")} \xB7 ${formatDate(c.last_message_at)}</span>
                </button>
                ${canDelete?`<button type="button" class="chat-visitor-delete" data-delete-id="${c.id}" data-label="${escapeHtml(label)}" title="Delete this conversation" aria-label="Delete the conversation with ${escapeHtml(label)}">&times;</button>`:""}
              </div>`}).join(""):`<p class="dev-empty">No one has started a chat yet. The bubble is live on every page of the site.</p>`;const convo=convos.find(c=>c.id===activeId);const msgs=activeId?getVisitorMessages(activeId):[];const threadHtml=convo?`
        <div class="chat-thread-header">
          ${escapeHtml(convo.visitor_name||"Anonymous visitor")}
          <span class="chat-live-pill">Live</span>
        </div>
        <div class="chat-messages" id="chatMessages">
          ${msgs.length?msgs.map(m=>`
              <div class="chat-message ${m.sender==="dev"?"mine":"theirs"}">
                <div class="chat-message-bubble">${escapeHtml(m.body)}</div>
                <span class="chat-message-meta">${escapeHtml(m.sender==="dev"?m.sender_name||"DigiCode":convo.visitor_name||"Visitor")} \xB7 ${formatDate(m.created_at)}</span>
              </div>`).join(""):`<p class="dev-empty">No messages yet.</p>`}
        </div>
        <form id="visitorSendForm" class="chat-input-row">
          <input type="text" id="visitorInput" class="chat-input" placeholder="Reply to this visitor\u2026" autocomplete="off" />
          <button type="submit" class="btn btn-primary">Send</button>
        </form>`:`<div class="chat-empty-state">Select a conversation to reply. New messages arrive here live.</div>`;panel.innerHTML=`
      <div class="chat-layout">
        <div class="chat-sidebar">
          <div class="billing-toggle chat-mode-toggle">
            <button type="button" class="billing-tab chat-mode-btn" data-mode="clients">Clients</button>
            <button type="button" class="billing-tab chat-mode-btn" data-mode="team">Talk to a Developer</button>
            <button type="button" class="billing-tab chat-mode-btn active" data-mode="visitors">Site Visitors${totalVisitorUnread2()?` (${totalVisitorUnread2()})`:""}</button>
          </div>
          <div class="chat-list">${listHtml}</div>
          ${canDelete&&convos.length?`<button type="button" class="btn btn-ghost btn-small chat-clear-read" id="clearReadChats">Clear read conversations</button>`:""}
        </div>
        <div class="chat-thread">${threadHtml}</div>
      </div>
    `;panel.querySelectorAll(".chat-mode-btn").forEach(btn=>{btn.addEventListener("click",()=>{chatListMode=btn.dataset.mode;activeChatId=null;renderChatPanel2()})});panel.querySelectorAll(".chat-list-item").forEach(btn=>{btn.addEventListener("click",()=>{activeChatId=btn.dataset.chatId;markVisitorSeen(Number(activeChatId.slice(8)));renderChatPanel2();const el=document.getElementById("chatMessages");if(el)el.scrollTop=el.scrollHeight})});panel.querySelectorAll(".chat-visitor-delete").forEach(btn=>{btn.addEventListener("click",e=>{e.stopPropagation();deleteVisitorConversation(Number(btn.dataset.deleteId),btn.dataset.label)})});document.getElementById("clearReadChats")?.addEventListener("click",deleteReadVisitorConversations);if(activeId)markVisitorSeen(activeId);const msgsEl=document.getElementById("chatMessages");if(msgsEl)msgsEl.scrollTop=msgsEl.scrollHeight;document.getElementById("visitorSendForm")?.addEventListener("submit",async e=>{e.preventDefault();const input=document.getElementById("visitorInput");const text=input.value.trim();if(!text||!activeId)return;input.value="";const{error}=await digicodeSupabase.from("visitor_messages").insert({conversation_id:activeId,sender:"dev",sender_name:resolvePortalFirstName2()||"DigiCode",body:text});if(error){input.value=text;return}await loadVisitorChats();renderChatPanel2()})},subscribeToVisitorChat2=function(){if(!digicodeSupabase.channel)return;digicodeSupabase.channel("visitor-chat-live").on("postgres_changes",{event:"INSERT",schema:"public",table:"visitor_messages"},async()=>{await loadVisitorChats();if(chatListMode==="visitors")renderChatPanel2();else renderChatPanel2()}).on("postgres_changes",{event:"*",schema:"public",table:"visitor_chats"},async()=>{await loadVisitorChats();renderChatPanel2()}).subscribe()},canManageDiscounts2=function(){const email=(window.digicodePortalEmail||"").toLowerCase();const me=getTeam().find(d=>(d.email||"").toLowerCase()===email);if(!me)return true;return me.rank==="Lead Developer"||(me.permissions||[]).includes("Manage Discount Codes")},codeStatus2=function(c){const now=Date.now();const start=new Date(c.starts_at).getTime();const end=new Date(c.expires_at).getTime();const exhausted=c.max_uses!=null&&c.times_used>=c.max_uses;if(!c.active)return{label:"Switched off",tone:"off"};if(exhausted)return{label:"Used up",tone:"off"};if(now<start)return{label:"Not started yet",tone:"wait"};if(now>=end)return{label:"Expired",tone:"off"};const hoursLeft=(end-now)/36e5;const left=hoursLeft>=48?`${Math.floor(hoursLeft/24)} days left`:hoursLeft>=2?`${Math.floor(hoursLeft)} hours left`:`${Math.max(1,Math.floor(hoursLeft*60))} min left`;return{label:left,tone:hoursLeft<48?"soon":"live"}},activeForLabel2=function(c){const days=(Date.now()-new Date(c.starts_at).getTime())/864e5;if(days<0)return"Scheduled";if(days<1)return`Live ${Math.max(1,Math.floor(days*24))}h`;return`Live ${Math.floor(days)} day${Math.floor(days)===1?"":"s"}`},canManageConcepts2=function(){const email=(window.digicodePortalEmail||"").toLowerCase();const me=getTeam().find(d=>(d.email||"").toLowerCase()===email);if(!me)return true;return me.rank==="Lead Developer"||(me.permissions||[]).includes("Manage Concepts")},conceptRowHtml2=function(c){const listed=c.status==="listed";const href=c.url||"concepts/"+c.slug+"/index.html";const manage=canManageConcepts2();return`
      <div class="project-card${listed?" is-paid":""}">
        <div class="project-card-head">
          <div>
            <span class="project-service">${escapeHtml(c.name)}</span>
            <span class="project-client">${escapeHtml(c.client)||"No client recorded"}</span>
          </div>
          <span class="project-date">${formatDate(c.created_at)}</span>
        </div>
        <div class="invoice-tag-row">
          <span class="invoice-tag ${listed?"tag-paid":"tag-void"}">${listed?"Listed publicly":"Hidden"}</span>
          <span class="invoice-tag tag-due">/${escapeHtml(c.slug)}</span>
        </div>
        ${c.summary?`<p class="project-details">${escapeHtml(c.summary)}</p>`:""}
        <p class="script-row-meta">${escapeHtml(href)}</p>
        <div class="project-actions">
          <a class="btn btn-primary btn-small-inline" href="${escapeHtml(href)}" target="_blank" rel="noopener">Open</a>
          ${manage?`<button type="button" class="clear-cart-link concept-toggle" data-id="${c.id}" data-to="${listed?"hidden":"listed"}">${listed?"Hide from site":"List on site"}</button>
                 <button type="button" class="clear-cart-link concept-edit" data-id="${c.id}">Edit</button>
                 <button type="button" class="clear-cart-link danger-link concept-delete" data-id="${c.id}" data-name="${escapeHtml(c.name)}">Delete</button>`:""}
        </div>
      </div>`},conceptFormHtml2=function(c){const e=c||{};return`
      <form class="concept-form" data-id="${e.id||""}">
        <h3 class="cancelled-head" style="margin-top:0;">${e.id?"Edit concept build":"Add a concept build"}</h3>
        <div class="concept-grid">
          <label>Name<input type="text" name="name" value="${escapeHtml(e.name)||""}" required placeholder="Client or project name" /></label>
          <label>Client<input type="text" name="client" value="${escapeHtml(e.client)||""}" placeholder="Business name" /></label>
          <label>Folder slug<input type="text" name="slug" value="${escapeHtml(e.slug)||""}" required placeholder="mummy-indays" /></label>
          <label>URL<input type="text" name="url" value="${escapeHtml(e.url)||""}" placeholder="concepts/mummy-indays/index.html" /></label>
        </div>
        <label class="concept-wide">Summary<textarea name="summary" rows="2" placeholder="One line on what it is and anything to watch.">${escapeHtml(e.summary)||""}</textarea></label>
        <label class="concept-wide">Status
          <select name="status">
            <option value="hidden"${e.status!=="listed"?" selected":""}>Hidden \u2014 not on the public page</option>
            <option value="listed"${e.status==="listed"?" selected":""}>Listed \u2014 shown as work</option>
          </select>
        </label>
        <div class="project-actions">
          <button type="submit" class="btn btn-primary btn-small-inline">${e.id?"Save changes":"Add build"}</button>
          ${e.id?`<button type="button" class="clear-cart-link concept-cancel">Cancel</button>`:""}
        </div>
      </form>`},renderConceptsPanel2=function(){const panel=document.getElementById("conceptsPanel");if(!panel)return;const manage=canManageConcepts2();const editing=conceptEditingId?conceptsCache.find(c=>c.id===conceptEditingId):null;panel.innerHTML=`<div class="orders-alert concepts-note">
         <strong>Hidden means unlisted, not private.</strong>
         A hidden build is kept off the public concepts page and out of search,
         but the site is static &mdash; anyone with the direct link can still open it.
         Deleting removes it from this register only; the folder stays in the repo
         until it is removed there too.
       </div>`+(manage?conceptFormHtml2(editing):"")+(conceptsCache.length?conceptsCache.map(conceptRowHtml2).join(""):`<p class="dev-empty">No concept builds registered yet.</p>`);panel.querySelectorAll(".concept-toggle").forEach(b=>{b.addEventListener("click",()=>setConceptStatus(Number(b.dataset.id),b.dataset.to))});panel.querySelectorAll(".concept-edit").forEach(b=>{b.addEventListener("click",()=>{conceptEditingId=Number(b.dataset.id);renderConceptsPanel2();const f=panel.querySelector(".concept-form");if(f)f.scrollIntoView({behavior:"smooth",block:"center"})})});panel.querySelectorAll(".concept-delete").forEach(b=>{b.addEventListener("click",()=>deleteConcept(Number(b.dataset.id),b.dataset.name))});const cancel=panel.querySelector(".concept-cancel");if(cancel){cancel.addEventListener("click",()=>{conceptEditingId=null;renderConceptsPanel2()})}const form=panel.querySelector(".concept-form");if(form)form.addEventListener("submit",saveConcept)},renderAllPanels2=function(){renderPendingPanel2();renderInvoicesPanel2();renderDiscountsPanel();renderAssignedPanel2();renderFinishedPanel2();renderDevelopersPanel2();renderChatPanel2();renderScriptsPanel2();renderScriptSalesPanel2();renderOrdersPanel2();renderConceptsPanel2();renderStatGrid2();renderRankList2();renderPerfGrid2()},waitForAuthReady2=function(){if(window.digicodePortalRole)return Promise.resolve();return new Promise(resolve=>document.addEventListener("digicode-auth-ready",resolve,{once:true}))},renderClientPreview2=function(){if(!clientViewContainer)return;const isRealClient=window.digicodePortalRole==="client";const projects=getProjects();const demoProject=projects[0];if(!demoProject){const invoicesOnly=clientInvoicesHtml2();clientViewContainer.innerHTML=isRealClient?(invoicesOnly||`<p class="dev-empty">We don't have a project on file for this account yet. If you've just purchased a build or plan, make sure you checked out with this same email \u2014 otherwise email <a href="mailto:developerteam@digi-code.com.au">developerteam@digi-code.com.au</a> and we'll sort it out.</p>`)+`<div style="text-align: center; margin-top: 24px;"><button type="button" class="clear-cart-link" data-portal-logout>Log out</button></div>`:`<p class="dev-empty">No projects exist yet to preview.</p>`;return}const chatId="client:"+demoProject.id;const chats=getChats();const msgs=chats[chatId]||[];const statusLabel=demoProject.status==="finished"?"Finished":demoProject.status==="assigned"?"In progress":"Submitted";const statusDate=demoProject.finishedAt||demoProject.assignedAt||demoProject.createdAt;const stageIndex=demoProject.status==="finished"?2:demoProject.status==="assigned"?1:0;const stages=["Submitted","In progress","Finished"];const trackerHtml=`
      <ol class="client-stage-track">
        ${stages.map((label,i)=>`
          <li class="client-stage ${i<stageIndex?"done":i===stageIndex?"current":""}">
            <span class="client-stage-dot"></span>
            <span class="client-stage-label">${label}</span>
          </li>`).join("")}
      </ol>`;const health=demoProject.health;const showHealth=health&&!health.loading&&!health.error&&health.performance!=null;const healthHtml=showHealth?`
      <h2 class="dashboard-section-title" style="margin-top: 40px;">Your Website Health</h2>
      <div class="health-panel">
        <div class="health-header">
          <span>Latest check${demoProject.domain?` \u2014 ${escapeHtml(demoProject.domain)}`:""}</span>
          <span class="health-meta">Checked ${formatDate(health.checkedAt)} \xB7 via Google PageSpeed Insights</span>
        </div>
        <div class="health-grid">
          <div class="stat-tile health-tile"><span class="stat-tile-label">Performance</span><span class="stat-tile-value ${scoreClass2(health.performance)}">${health.performance??"\u2014"}</span></div>
          <div class="stat-tile health-tile"><span class="stat-tile-label">SEO</span><span class="stat-tile-value ${scoreClass2(health.seo)}">${health.seo??"\u2014"}</span></div>
          <div class="stat-tile health-tile"><span class="stat-tile-label">Accessibility</span><span class="stat-tile-value ${scoreClass2(health.accessibility)}">${health.accessibility??"\u2014"}</span></div>
          <div class="stat-tile health-tile"><span class="stat-tile-label">Best Practices</span><span class="stat-tile-value ${scoreClass2(health.bestPractices)}">${health.bestPractices??"\u2014"}</span></div>
        </div>
        ${health.lcp?`<p class="health-vitals">LCP ${escapeHtml(health.lcp)} \xB7 CLS ${escapeHtml(health.cls)} \xB7 FCP ${escapeHtml(health.fcp)}</p>`:""}
        <p class="portal-note" style="margin: 10px 0 0;">Scored out of 100. We watch these as part of your plan and act on anything that slips.</p>
      </div>`:"";const review=demoProject.review;const reviewHtml=demoProject.status!=="finished"?"":`
      <h2 class="dashboard-section-title" style="margin-top: 40px;">${review?"Your Review":"How did we do?"}</h2>
      <div class="project-card">
        ${review?`<div class="project-review">
                 <span class="project-review-stars">${"\u2605".repeat(review.rating)}${"\u2606".repeat(5-review.rating)}</span>
                 ${review.text?`<p>"${escapeHtml(review.text)}"</p>`:""}
               </div>
               <button type="button" class="clear-cart-link" id="clientReviewEditBtn">Change my review</button>`:`<p class="portal-note" style="margin-top:0;">Your project's wrapped up \u2014 we'd love to know how it went.</p>`}
        <form id="clientReviewForm" class="portal-login-form" ${review?"hidden":""} style="margin-top: 14px;">
          <div class="form-group">
            <span class="form-label">Rating</span>
            <div class="client-star-row" id="clientStarRow">
              ${[1,2,3,4,5].map(n=>`<button type="button" class="client-star" data-rating="${n}" aria-label="${n} star${n>1?"s":""}">\u2605</button>`).join("")}
            </div>
          </div>
          <div class="form-group">
            <label for="clientReviewText">Anything you'd like to add? (optional)</label>
            <textarea id="clientReviewText" rows="3" placeholder="What was it like working with us?">${review&&review.text?escapeHtml(review.text):""}</textarea>
          </div>
          <p class="portal-login-error" id="clientReviewError" hidden></p>
          <button type="submit" class="btn btn-primary" id="clientReviewSubmit">${review?"Update Review":"Submit Review"}</button>
        </form>
      </div>`;clientViewContainer.innerHTML=`
      ${isRealClient?"":`<div class="portal-sample-banner">This is a simulated preview of "${escapeHtml(demoProject.clientName)}"'s view, using real pipeline data \u2014 for design/testing only.</div>`}

      <h2 class="dashboard-section-title">Your Project</h2>
      <div class="project-card">
        <div class="project-card-head">
          <div>
            <span class="project-service">${escapeHtml(demoProject.service)}</span>
            <span class="project-client">${escapeHtml(demoProject.clientName)}</span>
          </div>
          <span class="project-date">${statusLabel} ${formatDate(statusDate)}</span>
        </div>
        <div class="project-tiers">${tierTagsHtml2(demoProject)}</div>
        ${trackerHtml}
        ${demoProject.domain?`<p class="project-assigned-to">Your site: <a href="https://${escapeHtml(demoProject.domain)}" target="_blank" rel="noopener"><strong>${escapeHtml(demoProject.domain)}</strong></a></p>`:""}
        ${demoProject.assignedDev?`<p class="project-assigned-to">Your developer: <strong>${escapeHtml(demoProject.assignedDev)}</strong></p>`:`<p class="dev-empty">A developer hasn't been assigned yet.</p>`}
        ${demoProject.details?`<details class="client-brief"><summary>What you asked us for</summary><p>${escapeHtml(demoProject.details)}</p></details>`:""}
        <div class="project-notes">
          ${demoProject.progressNotes.length?demoProject.progressNotes.map(n=>`<div class="project-note"><span class="project-note-date">${formatDate(n.date)}</span><p>${escapeHtml(n.note)}</p></div>`).join(""):`<p class="dev-empty">No updates yet \u2014 your developer will post progress here as work happens.</p>`}
        </div>
      </div>
      ${healthHtml}
      ${reviewHtml}
      ${clientInvoicesHtml2()}

      <h2 class="dashboard-section-title" style="margin-top: 40px;">Message Your Developer</h2>
      <div class="chat-thread">
        <div class="chat-messages" id="clientChatMessages">
          ${msgs.length?msgs.map(m=>`
              <div class="chat-message ${m.from===(demoProject.clientName||"Client")?"mine":"theirs"}">
                <div class="chat-message-bubble">${escapeHtml(m.text)}</div>
                <span class="chat-message-meta">${escapeHtml(m.from)} \xB7 ${formatDate(m.at)}</span>
              </div>`).join(""):`<p class="dev-empty">No messages yet.</p>`}
        </div>
        <form id="clientChatSendForm" class="chat-input-row">
          <input type="text" id="clientChatInput" class="chat-input" placeholder="Type a message\u2026" autocomplete="off" />
          <button type="submit" class="btn btn-primary">Send</button>
        </form>
      </div>

      <div style="text-align: center; margin-top: 40px;">
        <button type="button" class="clear-cart-link" data-portal-logout>Log out</button>
      </div>
    `;let chosenRating=review?review.rating:0;const starRow=document.getElementById("clientStarRow");const paintStars=()=>{starRow?.querySelectorAll(".client-star").forEach(s=>{s.classList.toggle("on",Number(s.dataset.rating)<=chosenRating)})};paintStars();starRow?.querySelectorAll(".client-star").forEach(s=>{s.addEventListener("click",()=>{chosenRating=Number(s.dataset.rating);paintStars()})});document.getElementById("clientReviewEditBtn")?.addEventListener("click",()=>{const form=document.getElementById("clientReviewForm");if(form)form.hidden=false});document.getElementById("clientReviewForm")?.addEventListener("submit",async e=>{e.preventDefault();const errEl=document.getElementById("clientReviewError");const btn=document.getElementById("clientReviewSubmit");if(errEl)errEl.hidden=true;if(!chosenRating){if(errEl){errEl.textContent="Pick a star rating first.";errEl.hidden=false}return}if(btn){btn.disabled=true;btn.textContent="Sending\u2026"}const{error}=await digicodeSupabase.rpc("submit_project_review",{project_id:demoProject.id,rating:chosenRating,review_text:document.getElementById("clientReviewText").value.trim()});if(error){if(errEl){errEl.textContent="We couldn't save that just now \u2014 please try again.";errEl.hidden=false}if(btn){btn.disabled=false;btn.textContent="Submit Review"}return}await loadProjects();renderClientPreview2()});document.getElementById("clientChatSendForm")?.addEventListener("submit",async e=>{e.preventDefault();const input=document.getElementById("clientChatInput");const text=input.value.trim();if(!text)return;await digicodeSupabase.from("chats").insert({chat_id:chatId,from_name:demoProject.clientName||"Client",message:text});await loadChats();renderClientPreview2();const msgsEl=document.getElementById("clientChatMessages");if(msgsEl)msgsEl.scrollTop=msgsEl.scrollHeight})},firstNameOf2=function(full){if(!full)return null;const first=String(full).trim().split(/[\s,]+/)[0];if(!first)return null;return first.charAt(0).toUpperCase()+first.slice(1)},resolvePortalFirstName2=function(){const email=(window.digicodePortalEmail||"").toLowerCase();const fromProfile=firstNameOf2(window.digicodePortalName);if(fromProfile)return fromProfile;const dev=getTeam().find(d=>(d.email||"").toLowerCase()===email);const fromRoster=firstNameOf2(dev&&dev.name);if(fromRoster)return fromRoster;const project=getProjects().find(p=>(p.clientEmail||"").toLowerCase()===email);const fromProject=firstNameOf2(project&&project.clientName);if(fromProject)return fromProject;const local=email.split("@")[0]||"";return firstNameOf2(local.split(/[._\-+0-9]+/).filter(Boolean)[0])},applyPortalRole2=function(){const portalRole=window.digicodePortalRole;if(portalWelcomeName){const firstName=resolvePortalFirstName2();portalWelcomeName.textContent=firstName?`${firstName}.`:portalRole==="client"?"Client.":"Dev."}if(portalRole==="client"){if(viewSwitchRow)viewSwitchRow.hidden=true;if(devViewContainer)devViewContainer.hidden=true;if(clientViewContainer)clientViewContainer.hidden=false;const eyebrow=document.querySelector(".page-hero .eyebrow");const heroSub=document.querySelector(".page-hero .hero-sub");if(eyebrow)eyebrow.textContent="Your Portal";if(heroSub)heroSub.textContent="Track your project, see how your site's performing, and talk to your developer \u2014 all in one place.";renderClientPreview2()}},scriptImageUrl2=function(path){return path?`${SUPABASE_URL}/storage/v1/object/public/${SCRIPT_IMAGE_BUCKET}/${path}`:""},canManageScripts2=function(){const email=(window.digicodePortalEmail||"").toLowerCase();const me=getTeam().find(d=>(d.email||"").toLowerCase()===email);if(!me)return true;return me.rank==="Lead Developer"||(me.permissions||[]).includes("Manage Scripts")},scriptSize2=function(bytes){if(!bytes)return"no file";return bytes>1048576?(bytes/1048576).toFixed(1)+" MB":Math.round(bytes/1024)+" KB"},slugify2=function(s){return String(s||"").toLowerCase().replace(/[^a-z0-9]+/g,"-").replace(/^-+|-+$/g,"").slice(0,60)},currentDelivery2=function(s){if(!s)return"file";if(s.drive_url)return"drive";if(!s.file_path&&(s.cfx_details||"").trim())return"cfx";return"file"},scriptFormHtml2=function(s){const v=(k,d)=>escapeHtml(s&&s[k]!=null?s[k]:d||"");const category=s&&s.category||"Custom Build Scripts";const delivery=currentDelivery2(s);return`
      <form class="form-card script-form" id="scriptForm">
        <h2>${s?"Edit script":"List a new script"}</h2>
        <div class="form-grid">
          <div class="form-group">
            <label for="sfName">Name</label>
            <input type="text" id="sfName" value="${v("name")}" placeholder="Advanced Garage System" required />
          </div>
          <div class="form-group">
            <label for="sfCategory">Category</label>
            <select id="sfCategory">
              ${SCRIPT_CATEGORIES.map(c=>`<option value="${escapeHtml(c)}"${c===category?" selected":""}>${escapeHtml(c)}</option>`).join("")}
            </select>
          </div>
          <div class="form-group">
            <label for="sfPlatform">Platform</label>
            <input type="text" id="sfPlatform" value="${v("platform")}" placeholder="FiveM / Roblox / Minecraft" />
          </div>
          <div class="form-group">
            <label for="sfPrice">Price (AUD)</label>
            <input type="text" id="sfPrice" value="${s?(s.price_cents/100).toFixed(2):""}" placeholder="49.00" required />
          </div>
          <div class="form-group">
            <label for="sfVersion">Version</label>
            <input type="text" id="sfVersion" value="${v("version")}" placeholder="1.0.0" />
          </div>
        </div>
        <div class="form-group">
          <label for="sfSummary">One-line summary</label>
          <input type="text" id="sfSummary" value="${v("summary")}" placeholder="Shown on the store card \u2014 keep it to a sentence" />
        </div>
        <div class="form-group">
          <label for="sfDesc">Full description</label>
          <textarea id="sfDesc" rows="5" placeholder="What it does, what it needs to run, what's included. Buyers read this before they read the price.">${v("description")}</textarea>
        </div>
        <div class="form-group">
          <span class="form-label">How is it delivered?</span>
          <div class="radio-row">
            <label class="radio-option">
              <input type="radio" name="sfDelivery" value="file" ${delivery==="file"?"checked":""} />
              Upload the file (up to 50MB) \u2014 instant download
            </label>
            <label class="radio-option">
              <input type="radio" name="sfDelivery" value="drive" ${delivery==="drive"?"checked":""} />
              Google Drive link \u2014 instant download
            </label>
            <label class="radio-option">
              <input type="radio" name="sfDelivery" value="cfx" ${delivery==="cfx"?"checked":""} />
              CFX transfer \u2014 you send it by hand via cfx.re
            </label>
          </div>
        </div>

        <div class="form-group" id="sfFileGroup" ${delivery==="file"?"":"hidden"}>
          <span class="form-label">Script file (.zip)</span>
          <input type="file" id="sfFile" accept=".zip,.rar,.7z" />
          <p class="form-note" id="sfFileNote">${s&&s.file_name?"Currently: "+escapeHtml(s.file_name)+" \xB7 "+scriptSize2(s.file_bytes)+". Choose a file only to replace it.":"Up to 50MB (the Supabase plan limit). This is what the buyer downloads."}</p>
        </div>

        <div class="form-group" id="sfDriveGroup" ${delivery==="drive"?"":"hidden"}>
          <label for="sfDrive">Google Drive link</label>
          <input type="url" id="sfDrive" value="${escapeHtml(s&&s.drive_url||"")}"
                 placeholder="https://drive.google.com/file/d/..." />
          <p class="form-note">
            Set the file's sharing to <strong>Anyone with the link</strong> in Drive, or buyers
            won't be able to open it. Revealed only after payment \u2014 but unlike an uploaded file,
            a Drive link can't expire, so anyone a buyer passes it to can download it too.
          </p>
        </div>

        <div class="form-group cfx-listing-group" id="sfCfxGroup" ${delivery==="cfx"?"":"hidden"}>
          <label for="sfCfxDetails">Which cfx.re asset is this?</label>
          <textarea id="sfCfxDetails" rows="2" placeholder="e.g. asset name or link on your cfx.re Keymaster/assets page \u2014 for your own reference when it sells">${v("cfx_details")}</textarea>
          <p class="form-note">
            Not shown to buyers \u2014 just enough for you to find the right asset when it's time to
            transfer it. No file or Drive link for this route: a buyer gives their cfx.re account
            name at checkout, and you complete the transfer yourself in cfx.re. This is a
            <strong>one-off</strong> \u2014 the listing comes off the store the moment it sells, since
            there's only one licence to give away.
          </p>
        </div>
        <div class="form-group">
          <span class="form-label">Thumbnail</span>
          <div class="script-image-row">
            <div class="script-image-preview" id="sfImagePreview">
              ${s&&s.image_path?`<img src="${escapeHtml(scriptImageUrl2(s.image_path))}" alt="" />`:`<span>No image</span>`}
            </div>
            <div class="script-image-controls">
              <input type="file" id="sfImage" accept="image/png,image/jpeg,image/webp,image/gif" />
              <p class="form-note">Shown on the store card. Landscape works best \u2014 around 800&times;450. Up to 5MB.</p>
              ${s&&s.image_path?`<button type="button" class="clear-cart-link danger-link" id="sfImageClear">Remove image</button>`:""}
            </div>
          </div>
        </div>
        <div class="form-group">
          <label class="checkbox-option">
            <input type="checkbox" id="sfActive" ${s&&s.active?"checked":""} />
            Listed for sale on the store
          </label>
        </div>
        <div class="form-actions" style="padding:0;">
          <button type="submit" class="btn btn-primary" id="sfSave">${s?"Save changes":"List script"}</button>
          ${s?`<button type="button" class="clear-cart-link" id="sfCancel">Cancel</button>`:""}
          <p class="form-note" id="sfStatus"></p>
        </div>
      </form>`},renderScriptsPanel2=function(){const panel=document.getElementById("scriptsPanel");if(!panel)return;if(!canManageScripts2()){panel.innerHTML=`<p class="dev-empty">Only the Lead Developer can list scripts for sale.</p>`;return}const editing=editingScriptId?scriptsCache.find(s=>String(s.id)===String(editingScriptId)):null;const rows=scriptsCache.length?scriptsCache.map(s=>`
        <div class="project-card${s.active?" is-paid":""}">
          <div class="project-card-head">
            <div class="script-admin-head">
              ${s.image_path?`<img class="script-admin-thumb" src="${escapeHtml(scriptImageUrl2(s.image_path))}" alt="" />`:""}
              <div>
                <span class="project-service">${escapeHtml(s.category||"Custom Build Scripts")}${s.platform?" \xB7 "+escapeHtml(s.platform):""}${s.version?" \xB7 v"+escapeHtml(s.version):""}</span>
                <span class="project-client">${escapeHtml(s.name)}</span>
              </div>
            </div>
            <span class="project-date">$${(s.price_cents/100).toFixed(2)}</span>
          </div>
          <div class="order-flag ${s.sold_at?"flag-quote":s.active?"flag-paid":"flag-enquiry"}">
            ${s.sold_at?"Sold "+formatDate(s.sold_at):s.active?"Live on the store":"Draft \u2014 not listed"}
          </div>
          ${s.summary?`<p class="project-details">${escapeHtml(s.summary)}</p>`:""}
          <p class="script-row-meta">${s.drive_url?`<span class="script-route">Google Drive</span> \xB7 link revealed on purchase`:s.file_path?`${escapeHtml(s.file_name||"no file uploaded")} \xB7 ${scriptSize2(s.file_bytes)}`:`<span class="script-route">cfx.re transfer</span> \xB7 ${escapeHtml(s.cfx_details||"no asset noted")}`} \xB7 ${s.sales_count} sold</p>
          <div class="project-actions">
            <button type="button" class="clear-cart-link script-edit" data-id="${s.id}">Edit</button>
            <button type="button" class="clear-cart-link script-toggle" data-id="${s.id}">${s.active?"Unlist":"List for sale"}</button>
            <button type="button" class="clear-cart-link danger-link script-delete" data-id="${s.id}">Delete</button>
          </div>
        </div>`).join(""):`<p class="dev-empty">No scripts listed yet.</p>`;panel.innerHTML=scriptFormHtml2(editing)+`<div class="script-admin-list">${rows}</div>`;panel.querySelector("#scriptForm").addEventListener("submit",e=>{e.preventDefault();saveScript(editing)});const imgInput=panel.querySelector("#sfImage");imgInput?.addEventListener("change",()=>{const f=imgInput.files[0];const box=panel.querySelector("#sfImagePreview");if(!f||!box)return;const url=URL.createObjectURL(f);box.innerHTML=`<img src="${url}" alt="" />`});panel.querySelector("#sfImageClear")?.addEventListener("click",()=>{clearScriptImage(editing)});function applyDeliveryVisibility(value){panel.querySelector("#sfFileGroup").hidden=value!=="file";panel.querySelector("#sfDriveGroup").hidden=value!=="drive";panel.querySelector("#sfCfxGroup").hidden=value!=="cfx"}panel.querySelectorAll('input[name="sfDelivery"]').forEach(r=>{r.addEventListener("change",()=>{if(r.checked)applyDeliveryVisibility(r.value)})});panel.querySelector("#sfCategory")?.addEventListener("change",e=>{if(editing)return;if(!CFX_CATEGORIES.includes(e.target.value))return;const cfxRadio=panel.querySelector('input[name="sfDelivery"][value="cfx"]');if(!cfxRadio)return;cfxRadio.checked=true;applyDeliveryVisibility("cfx")});panel.querySelector("#sfCancel")?.addEventListener("click",()=>{editingScriptId=null;renderScriptsPanel2()});panel.querySelectorAll(".script-edit").forEach(b=>b.addEventListener("click",()=>{editingScriptId=b.dataset.id;renderScriptsPanel2();panel.scrollIntoView({behavior:"smooth",block:"start"})}));panel.querySelectorAll(".script-toggle").forEach(b=>b.addEventListener("click",()=>toggleScript(b.dataset.id)));panel.querySelectorAll(".script-delete").forEach(b=>b.addEventListener("click",()=>deleteScript(b.dataset.id)))},minutesSince2=function(ts){if(!ts)return 0;return Math.max(0,Math.round((Date.now()-new Date(ts).getTime())/6e4))},ageLabel2=function(ts){const mins=minutesSince2(ts);if(mins<1)return"just now";if(mins<60)return mins+" min ago";const hours=Math.round(mins/60);if(hours<24)return hours+(hours===1?" hour ago":" hours ago");const days=Math.round(hours/24);return days+(days===1?" day ago":" days ago")},isStuck2=function(o){return o.status==="pending"&&minutesSince2(o.created_at)>=STUCK_MINUTES},orderItemsSummary2=function(o){const items=Array.isArray(o.items)?o.items:[];if(!items.length)return"\u2014";return items.map(l=>escapeHtml(l.name||l.sku||"item")+(Number(l.qty)>1?" \xD7"+Number(l.qty):"")).join(" \xB7 ")},orderRowHtml2=function(o){const stuck=isStuck2(o);const paid=o.status==="paid";const actions=[!paid?`<button type="button" class="btn btn-primary btn-small-inline order-confirm" data-ref="${escapeHtml(o.reference)}">Check with Stripe</button>`:"",canRemoveProjects2()?`<button type="button" class="clear-cart-link danger-link order-delete"
              data-ref="${escapeHtml(o.reference)}"
              data-paid="${paid?"1":""}"
              data-label="${orderItemsSummary2(o)}">Delete</button>`:""].filter(Boolean);return`
      <div class="project-card${paid?" is-paid":stuck?" is-cancelled":""}">
        <div class="project-card-head">
          <div>
            <span class="project-service">${orderItemsSummary2(o)}</span>
            <span class="project-client">${escapeHtml(o.customer_email)||"no email given"}</span>
          </div>
          <span class="project-date">${money2(o.total_cents)}</span>
        </div>
        <div class="invoice-tag-row">
          <span class="invoice-tag ${paid?"tag-paid":stuck?"tag-void":"tag-sent"}">${paid?"Paid":stuck?"Stuck \u2014 not confirmed":"Awaiting payment"}</span>
          <span class="invoice-tag tag-due">${ageLabel2(o.created_at)}</span>
        </div>
        ${stuck?`<p class="project-details">This has been waiting ${minutesSince2(o.created_at)} minutes.
                 If the customer did pay, Stripe knows \u2014 check and settle it here.</p>`:""}
        <p class="script-row-meta">Reference: ${escapeHtml(o.reference)}${o.paid_at?" \xB7 paid "+formatDate(o.paid_at):""}</p>
        ${actions.length?`<div class="project-actions">${actions.join("")}</div>`:""}
      </div>`},renderOrdersPanel2=function(){const panel=document.getElementById("ordersPanel");if(!panel)return;const stuck=ordersCache.filter(isStuck2);const alertEl=document.getElementById("ordersTabAlert");if(alertEl){alertEl.hidden=stuck.length===0;alertEl.textContent=stuck.length?String(stuck.length):""}if(!ordersCache.length){panel.innerHTML=`<p class="dev-empty">No orders yet.</p>`;return}const rest=ordersCache.filter(o=>!isStuck2(o));panel.innerHTML=(stuck.length?`<div class="orders-alert">
             <strong>${stuck.length} order${stuck.length>1?"s":""} stuck unconfirmed.</strong>
             A payment that went through but never reached us leaves the customer with nothing.
             Press "Check with Stripe" to settle one \u2014 it only completes if Stripe says the money
             actually arrived.
           </div>`+stuck.map(orderRowHtml2).join("")+`<h3 class="cancelled-head" style="margin-top: 30px;">Everything else (${rest.length})</h3>`:"")+rest.map(orderRowHtml2).join("");panel.querySelectorAll(".order-confirm").forEach(btn=>{btn.addEventListener("click",()=>confirmOrderFromPortal(btn))});panel.querySelectorAll(".order-delete").forEach(btn=>{btn.addEventListener("click",()=>deleteOrderFromPortal(btn))})},scriptSaleRowHtml2=function(sale){const isCfx=sale.delivery==="cfx";const done=!!sale.fulfilled_at;return`
      <div class="project-card${done?" is-paid":isCfx?" is-quote":""}">
        <div class="project-card-head">
          <div>
            <span class="project-service">${escapeHtml(sale.category||"Script")}</span>
            <span class="project-client">${escapeHtml(sale.script_name)}</span>
          </div>
          <span class="project-date">${money2(sale.price_cents)}</span>
        </div>
        <div class="invoice-tag-row">
          <span class="invoice-tag ${isCfx?"tag-due":"tag-paid"}">${isCfx?"cfx.re transfer":"Instant download"}</span>
          ${isCfx?done?`<span class="invoice-tag tag-paid">Sent ${formatDate(sale.fulfilled_at)}</span>`:`<span class="invoice-tag tag-sent">Owed</span>`:""}
        </div>
        <dl class="project-contact">
          <div><dt>Buyer</dt><dd>${escapeHtml(sale.buyer_email)||"\u2014"}</dd></div>
          ${isCfx?`<div><dt>cfx.re account</dt><dd>${escapeHtml((sale.cfx_account||"").replace(/^cfx\.re account:\s*/i,""))||"not given"}</dd></div>`:`<div><dt>Downloads</dt><dd>${escapeHtml(sale.downloads)}</dd></div>`}
        </dl>
        ${isCfx?`<p class="project-details">${sale.cfx_details?"Send: "+escapeHtml(sale.cfx_details):"No cfx.re asset noted on this listing."}</p>`:""}
        <p class="script-row-meta">Sold ${formatDate(sale.purchased_at)}</p>
        ${isCfx&&!done?`<div class="project-actions">
                 <button type="button" class="btn btn-primary btn-small-inline script-sale-fulfil" data-id="${sale.purchase_id}">Mark as sent</button>
               </div>`:""}
      </div>`},renderScriptSalesPanel2=function(){const panel=document.getElementById("scriptSalesPanel");if(!panel)return;if(!canManageScripts2()){panel.innerHTML=`<p class="dev-empty">Only the Lead Developer can see script sales.</p>`;return}if(!scriptSalesCache.length){panel.innerHTML=`<p class="dev-empty">No scripts sold yet.</p>`;return}const owed=scriptSalesCache.filter(s=>s.delivery==="cfx"&&!s.fulfilled_at);const rest=scriptSalesCache.filter(s=>!(s.delivery==="cfx"&&!s.fulfilled_at));panel.innerHTML=(owed.length?`<h3 class="cancelled-head">cfx.re transfers owed (${owed.length})</h3>`+owed.map(scriptSaleRowHtml2).join("")+`<h3 class="cancelled-head" style="margin-top: 30px;">All sales (${scriptSalesCache.length})</h3>`:`<h3 class="cancelled-head">All sales (${scriptSalesCache.length})</h3>`)+rest.map(scriptSaleRowHtml2).join("");panel.querySelectorAll(".script-sale-fulfil").forEach(btn=>{btn.addEventListener("click",()=>markScriptSaleFulfilled(btn.dataset.id))})},canPriceQuotes2=function(){const email=(window.digicodePortalEmail||"").toLowerCase();const me=getTeam().find(d=>(d.email||"").toLowerCase()===email);if(!me)return true;return me.rank==="Lead Developer"||(me.permissions||[]).includes("Price Quotes")},money2=function(cents){return"$"+((Number(cents)||0)/100).toLocaleString("en-AU",{minimumFractionDigits:2,maximumFractionDigits:2})},invoiceDate2=function(d){if(!d)return"";const parts=String(d).slice(0,10).split("-");if(parts.length!==3)return String(d);const dt=new Date(Number(parts[0]),Number(parts[1])-1,Number(parts[2]));if(isNaN(dt))return String(d);return dt.toLocaleDateString("en-AU",{day:"numeric",month:"short",year:"numeric"})},toCents2=function(dollars){const n=Number(String(dollars).replace(/[^0-9.]/g,""));return isFinite(n)?Math.round(n*100):0},invoicePayUrl2=function(ref){return location.origin+location.pathname.replace(/[^/]*$/,"")+"invoice.html?ref="+ref},blankDraft2=function(project){return{id:null,projectId:project?project.id:null,clientName:project?project.clientName||"":"",clientEmail:project?project.clientEmail||"":"",title:project?"Quote \u2014 "+(project.service||"Custom work"):"Quote",lines:[{description:project?project.service||"":"",qty:1,dollars:""}],notes:"",dueAt:"",discount:""}},draftFromInvoice2=function(inv){return{id:inv.id,projectId:inv.project_id,clientName:inv.client_name||"",clientEmail:inv.client_email||"",title:inv.title||"Quote",lines:(inv.lines||[]).map(l=>({description:l.description||"",qty:Number(l.qty)||1,dollars:((Number(l.unit_cents)||0)/100).toFixed(2)})),notes:inv.notes||"",dueAt:inv.due_at||"",discount:inv.discount_cents?(inv.discount_cents/100).toFixed(2):""}},lineWillSave2=function(l){return!!(l.description||"").trim()},draftTotals2=function(d){const subtotal=d.lines.reduce((sum,l)=>sum+(lineWillSave2(l)?toCents2(l.dollars)*(Number(l.qty)||1):0),0);const discount=Math.min(subtotal,toCents2(d.discount));return{subtotal,discount,total:subtotal-discount}},invoiceLineLabel2=function(l){const cents=l.line_cents!=null?l.line_cents:(l.unit_cents||0)*(l.qty||1);return money2(cents)},invoiceFormHtml2=function(d){const t=draftTotals2(d);const lineRows=d.lines.map((l,i)=>`
      <div class="invoice-line-row" data-i="${i}">
        <input type="text" class="inv-desc" placeholder="What it's for" value="${escapeHtml(l.description)}" />
        <input type="number" class="inv-qty" min="1" max="999" step="1" value="${Number(l.qty)||1}" aria-label="Quantity" />
        <div class="inv-price-wrap"><span>$</span><input type="text" class="inv-price" inputmode="decimal" placeholder="0.00" value="${escapeHtml(l.dollars)}" aria-label="Unit price" /></div>
        <button type="button" class="inv-line-remove" data-i="${i}" aria-label="Remove line"${d.lines.length===1?" disabled":""}>&times;</button>
      </div>`).join("");return`
      <form class="form-card invoice-builder" id="invoiceForm">
        <h2>${d.id?"Edit invoice":"Price this quote"}</h2>

        <div class="form-grid">
          <div class="form-group">
            <label for="ivTitle">Title</label>
            <input type="text" id="ivTitle" value="${escapeHtml(d.title)}" placeholder="Quote \u2014 Business website" />
          </div>
          <div class="form-group">
            <label for="ivDue">Due by <span class="form-optional">(optional)</span></label>
            <input type="date" id="ivDue" value="${escapeHtml(d.dueAt)}" />
          </div>
          <div class="form-group">
            <label for="ivName">Client name</label>
            <input type="text" id="ivName" value="${escapeHtml(d.clientName)}" placeholder="Jane Smith" />
          </div>
          <div class="form-group">
            <label for="ivEmail">Client email</label>
            <input type="email" id="ivEmail" value="${escapeHtml(d.clientEmail)}" placeholder="jane@example.com" />
          </div>
        </div>

        <h3 class="invoice-lines-head">What they're paying for</h3>
        <div class="invoice-line-head">
          <span>Description</span><span>Qty</span><span>Unit price</span><span></span>
        </div>
        <div id="invoiceLines">${lineRows}</div>
        <button type="button" class="btn btn-ghost btn-small" id="ivAddLine">+ Add another service</button>

        <div class="form-grid" style="margin-top: 22px;">
          <div class="form-group">
            <label for="ivDiscount">Discount <span class="form-optional">(optional, in dollars)</span></label>
            <div class="inv-price-wrap"><span>$</span><input type="text" id="ivDiscount" inputmode="decimal" placeholder="0.00" value="${escapeHtml(d.discount)}" /></div>
          </div>
        </div>

        <div class="form-group">
          <label for="ivNotes">Notes for the client <span class="form-optional">(optional)</span></label>
          <textarea id="ivNotes" rows="3" placeholder="What's included, timeframes, anything they should know before paying.">${escapeHtml(d.notes)}</textarea>
        </div>

        <div class="invoice-builder-total">
          <div><span>Subtotal</span><strong id="ivSubtotal">${money2(t.subtotal)}</strong></div>
          <div${t.discount?"":' hidden id="ivDiscountRow"'}${t.discount?' id="ivDiscountRow"':""}><span>Discount</span><strong id="ivDiscountShown">\u2212${money2(t.discount)}</strong></div>
          <div class="invoice-builder-grand"><span>Total</span><strong id="ivTotal">${money2(t.total)}</strong></div>
        </div>

        <p class="portal-login-error" id="ivError" hidden></p>

        <div class="invoice-builder-actions">
          <button type="submit" class="btn btn-primary" id="ivSave">${d.id?"Save changes":"Save draft"}</button>
          <button type="button" class="clear-cart-link" id="ivCancel">Cancel</button>
        </div>
        <p class="form-note">Saving keeps it as a draft \u2014 the client sees nothing until you send it.</p>
      </form>`},invoiceStatusTag2=function(inv){const map={draft:["Draft","tag-draft"],sent:["Sent","tag-sent"],paid:["Paid","tag-paid"],void:["Cancelled","tag-void"]};const pair=map[inv.status]||["\u2014",""];return'<span class="invoice-tag '+pair[1]+'">'+pair[0]+"</span>"},invoiceRowHtml2=function(inv){const sent=inv.status==="sent";const paid=inv.status==="paid";const voided=inv.status==="void";const url=invoicePayUrl2(inv.reference);return`
      <div class="project-card invoice-card${paid?" is-paid":voided?" is-cancelled":""}">
        <div class="project-card-head">
          <div>
            <span class="project-service">${escapeHtml(inv.title)}</span>
            <span class="project-client">${escapeHtml(inv.client_name)||"No name"}${inv.client_email?" \xB7 "+escapeHtml(inv.client_email):""}</span>
          </div>
          <span class="project-date">${money2(inv.total_cents)}</span>
        </div>

        <div class="invoice-tag-row">
          ${invoiceStatusTag2(inv)}
          ${inv.due_at&&!paid?`<span class="invoice-tag tag-due">Due ${invoiceDate2(inv.due_at)}</span>`:""}
        </div>

        <ul class="invoice-mini-lines">
          ${(inv.lines||[]).map(l=>`<li><span>${escapeHtml(l.description)}${Number(l.qty)>1?" \xD7 "+Number(l.qty):""}</span><span>${invoiceLineLabel2(l)}</span></li>`).join("")}
        </ul>

        ${inv.notes?`<p class="project-details">${escapeHtml(inv.notes)}</p>`:""}

        ${paid?`<p class="project-assigned-to">Paid ${formatDate(inv.paid_at)}</p>`:voided?`<p class="dev-empty">Cancelled \u2014 the pay link no longer works.</p>`:sent?`<p class="project-assigned-to">Sent ${formatDate(inv.sent_at)}${inv.delivery?" \xB7 by "+escapeHtml(inv.delivery==="both"?"email and portal":inv.delivery):""}</p>
                   <div class="invoice-link-row">
                     <input type="text" class="invoice-link" readonly value="${escapeHtml(url)}" aria-label="Payment link" />
                     <button type="button" class="btn btn-ghost btn-small iv-copy" data-url="${escapeHtml(url)}">Copy link</button>
                   </div>`:`<p class="dev-empty">Not sent yet \u2014 the client can't see this.</p>`}

        <div class="project-actions invoice-actions-row">
          ${!paid&&!voided?`<button type="button" class="btn btn-primary btn-small-inline iv-send" data-id="${inv.id}">${sent?"Send again":"Send"}</button>
                 <button type="button" class="clear-cart-link iv-edit" data-id="${inv.id}">Edit</button>
                 <button type="button" class="clear-cart-link iv-void" data-id="${inv.id}">Cancel invoice</button>`:""}
          ${paid?`<button type="button" class="clear-cart-link iv-copy" data-url="${escapeHtml(url)}">Copy receipt link</button>`:""}
          ${!paid?`<button type="button" class="clear-cart-link danger-link iv-delete" data-id="${inv.id}">Delete</button>`:""}
        </div>
      </div>`},renderInvoicesPanel2=function(){const panel=document.getElementById("invoicesPanel");if(!panel)return;if(!canPriceQuotes2()){panel.innerHTML=`<p class="dev-empty">Pricing and sending quotes needs the Lead Developer rank or the "Price Quotes" permission.</p>`;return}const formHtml=invoiceDraft?invoiceFormHtml2(invoiceDraft):"";const invoiced=new Set(invoicesCache.filter(i=>i.status!=="void").map(i=>i.project_id));const waiting=getProjects().filter(p=>p.orderKind==="quote"&&p.status!=="cancelled"&&!invoiced.has(p.id));const waitingHtml=waiting.length?`<div class="invoice-waiting">
           <h3 class="cancelled-head">Quotes waiting on a price (${waiting.length})</h3>
           ${waiting.map(p=>`
             <div class="project-card is-quote">
               <div class="project-card-head">
                 <div>
                   <span class="project-service">${escapeHtml(p.service)}</span>
                   <span class="project-client">${escapeHtml(p.clientName)||"No name provided"}${p.clientEmail?" \xB7 "+escapeHtml(p.clientEmail):""}</span>
                 </div>
                 <span class="project-date">Asked ${formatDate(p.createdAt)}</span>
               </div>
               <p class="project-details">${escapeHtml(p.details)||"No details provided."}</p>
               <button type="button" class="btn btn-primary btn-small-inline iv-price" data-project="${p.id}">Price this quote</button>
             </div>`).join("")}
         </div>`:"";const listHtml=invoicesCache.length?invoicesCache.map(invoiceRowHtml2).join(""):`<p class="dev-empty">No invoices yet. Price a quote above, or start one from scratch.</p>`;panel.innerHTML=formHtml+(invoiceDraft?"":`<button type="button" class="btn btn-ghost" id="ivNew" style="margin-bottom: 26px;">+ New invoice</button>`)+waitingHtml+`<h3 class="cancelled-head" style="margin-top: 30px;">All invoices (${invoicesCache.length})</h3>`+listHtml;wireInvoicePanel2(panel)},readDraftFromForm2=function(){if(!invoiceDraft)return null;const d=invoiceDraft;const val=id=>{const el=document.getElementById(id);return el?el.value:""};if(!document.getElementById("invoiceForm"))return d;d.title=val("ivTitle");d.clientName=val("ivName");d.clientEmail=val("ivEmail");d.notes=val("ivNotes");d.dueAt=val("ivDue");d.discount=val("ivDiscount");d.lines=[...document.querySelectorAll(".invoice-line-row")].map(row=>{const pick=sel=>{const el=row.querySelector(sel);return el?el.value:""};return{description:pick(".inv-desc"),qty:Number(pick(".inv-qty"))||1,dollars:pick(".inv-price")}});return d},refreshDraftTotals2=function(){if(!invoiceDraft)return;readDraftFromForm2();const t=draftTotals2(invoiceDraft);const set=(id,txt)=>{const el=document.getElementById(id);if(el)el.textContent=txt};set("ivSubtotal",money2(t.subtotal));set("ivDiscountShown","\u2212"+money2(t.discount));set("ivTotal",money2(t.total));const row=document.getElementById("ivDiscountRow");if(row)row.hidden=!t.discount},wireInvoicePanel2=function(panel){const newBtn=panel.querySelector("#ivNew");if(newBtn){newBtn.addEventListener("click",()=>{invoiceDraft=blankDraft2(null);renderInvoicesPanel2()})}panel.querySelectorAll(".iv-price").forEach(btn=>{btn.addEventListener("click",()=>{const p=getProjects().find(x=>x.id===btn.dataset.project);invoiceDraft=blankDraft2(p);renderInvoicesPanel2();const form2=document.getElementById("invoiceForm");if(form2)form2.scrollIntoView({behavior:"smooth",block:"start"})})});panel.querySelectorAll(".iv-edit").forEach(btn=>{btn.addEventListener("click",()=>{const inv=invoicesCache.find(i=>String(i.id)===String(btn.dataset.id));if(!inv)return;invoiceDraft=draftFromInvoice2(inv);renderInvoicesPanel2();const form2=document.getElementById("invoiceForm");if(form2)form2.scrollIntoView({behavior:"smooth",block:"start"})})});panel.querySelectorAll(".iv-send").forEach(btn=>{btn.addEventListener("click",()=>sendInvoice(btn.dataset.id))});panel.querySelectorAll(".iv-void").forEach(btn=>{btn.addEventListener("click",()=>voidInvoice(btn.dataset.id))});panel.querySelectorAll(".iv-delete").forEach(btn=>{btn.addEventListener("click",()=>deleteInvoice(btn.dataset.id))});panel.querySelectorAll(".iv-copy").forEach(btn=>{btn.addEventListener("click",async()=>{try{await navigator.clipboard.writeText(btn.dataset.url);const was=btn.textContent;btn.textContent="Copied";setTimeout(()=>{btn.textContent=was},1600)}catch(e){window.prompt("Copy this link:",btn.dataset.url)}})});const form=panel.querySelector("#invoiceForm");if(!form)return;form.addEventListener("input",refreshDraftTotals2);const addBtn=panel.querySelector("#ivAddLine");if(addBtn){addBtn.addEventListener("click",()=>{readDraftFromForm2();invoiceDraft.lines.push({description:"",qty:1,dollars:""});renderInvoicesPanel2()})}panel.querySelectorAll(".inv-line-remove").forEach(btn=>{btn.addEventListener("click",()=>{readDraftFromForm2();invoiceDraft.lines.splice(Number(btn.dataset.i),1);if(!invoiceDraft.lines.length){invoiceDraft.lines.push({description:"",qty:1,dollars:""})}renderInvoicesPanel2()})});const cancelBtn=panel.querySelector("#ivCancel");if(cancelBtn){cancelBtn.addEventListener("click",()=>{invoiceDraft=null;renderInvoicesPanel2()})}form.addEventListener("submit",async e=>{e.preventDefault();await saveInvoice()})},invoiceEmail2=function(inv){const url=invoicePayUrl2(inv.reference);const itemLines=(inv.lines||[]).map(l=>"  - "+l.description+(Number(l.qty)>1?" x "+l.qty:"")+" \u2014 "+invoiceLineLabel2(l)).join("\n");const subject="Your DigiCode quote \u2014 "+money2(inv.total_cents);const body="Hi "+(inv.client_name||"there")+",\n\nThanks for your patience \u2014 here's what we've put together.\n\n"+inv.title+"\n\n"+itemLines+"\n"+(inv.discount_cents?"  Discount \u2014 -"+money2(inv.discount_cents)+"\n":"")+"\nTotal: "+money2(inv.total_cents)+" AUD\n"+(inv.due_at?"Due by: "+invoiceDate2(inv.due_at)+"\n":"")+(inv.notes?"\n"+inv.notes+"\n":"")+"\nYou can look it over and pay securely by card here:\n"+url+"\n\nAny questions at all, just reply to this email.\n\nThanks,\nDigiCode\nhttps://www.digi-code.com.au";return{to:inv.client_email,subject,body}},invoiceMailto2=function(inv){const e=invoiceEmail2(inv);return"mailto:"+encodeURIComponent(e.to)+"?subject="+encodeURIComponent(e.subject)+"&body="+encodeURIComponent(e.body)},clientInvoicesHtml2=function(){const mine=invoicesCache.filter(i=>i.status==="sent"||i.status==="paid");if(!mine.length)return"";return'<h2 class="dashboard-section-title" style="margin-top: 40px;">Invoices</h2><div class="client-invoice-list">'+mine.map(inv=>{const paid=inv.status==="paid";return`
        <div class="project-card invoice-card${paid?" is-paid":""}">
          <div class="project-card-head">
            <div>
              <span class="project-service">${escapeHtml(inv.title)}</span>
              <span class="project-client">${paid?"Paid "+formatDate(inv.paid_at):"Sent "+formatDate(inv.sent_at)}</span>
            </div>
            <span class="project-date">${money2(inv.total_cents)}</span>
          </div>
          <div class="invoice-tag-row">
            ${invoiceStatusTag2(inv)}
            ${inv.due_at&&!paid?`<span class="invoice-tag tag-due">Due ${invoiceDate2(inv.due_at)}</span>`:""}
          </div>
          <ul class="invoice-mini-lines">
            ${(inv.lines||[]).map(l=>`<li><span>${escapeHtml(l.description)}${Number(l.qty)>1?" \xD7 "+Number(l.qty):""}</span><span>${invoiceLineLabel2(l)}</span></li>`).join("")}
          </ul>
          ${inv.notes?`<p class="project-details">${escapeHtml(inv.notes)}</p>`:""}
          <div class="project-actions">
            ${paid?`<a class="clear-cart-link" href="invoice.html?ref=${escapeHtml(inv.reference)}">View receipt</a>`:`<a class="btn btn-primary btn-small-inline" href="invoice.html?ref=${escapeHtml(inv.reference)}">Pay ${money2(inv.total_cents)} Now</a>`}
          </div>
        </div>`}).join("")+"</div>"},pctChange2=function(cur,prev){cur=Number(cur)||0;prev=Number(prev)||0;if(!prev)return cur>0?{dir:"up",text:"new"}:{dir:"flat",text:"\u2014"};const pct=(cur-prev)/prev*100;return{dir:pct>.05?"up":pct<-.05?"down":"flat",text:(pct>=0?"":"")+Math.abs(pct).toFixed(1)+"%"}},sparkPoints2=function(series){const vals=Array.isArray(series)&&series.length?series.map(n=>Number(n)||0):[];if(!vals.length)return null;const max=Math.max(...vals);const min=Math.min(...vals);const span=max-min||1;return vals.map((v,i)=>{const x=i/(vals.length-1||1)*100;const y=27-(v-min)/span*24;return{x:Math.round(x*10)/10,y:Math.round(y*10)/10}})},sparkSvg2=function(series){const pts=sparkPoints2(series);if(!pts)return"";const all=pts.map(p=>`${p.x},${p.y}`).join(" ");const tail=pts.slice(-2).map(p=>`${p.x},${p.y}`).join(" ");const last=pts[pts.length-1];return`<svg class="stat-sparkline" width="80" height="24" viewBox="0 0 100 30" aria-hidden="true">
                  <polyline class="spark-base" points="${all}" />
                  <polyline class="spark-accent" points="${tail}" />
                  <circle class="spark-dot" cx="${last.x}" cy="${last.y}" r="3" />
                </svg>`},statTile2=function(label,value,delta,series){const arrow=delta.dir==="down"?ARROW_DOWN:delta.dir==="up"?ARROW_UP:"";const cls=delta.dir==="flat"?"":` ${delta.dir}`;return`<div class="stat-tile">
              <span class="stat-tile-label">${escapeHtml(label)}</span>
              <span class="stat-tile-value">${escapeHtml(value)}</span>
              <div class="stat-tile-footer">
                <span class="stat-delta${cls}">${arrow}${escapeHtml(delta.text)}</span>
                ${sparkSvg2(series)}
              </div>
            </div>`},renderStatGrid2=function(){const grid=document.getElementById("statGrid");const stamp=document.getElementById("statsStamp");if(!grid)return;if(!siteStats){grid.innerHTML=`<p class="dev-empty">Couldn't load live stats. If this is the first time, run <code>supabase/022_site_analytics.sql</code> in the Supabase SQL editor.</p>`;if(stamp)stamp.textContent="";return}const s=siteStats;const spark=s.spark_sessions||[];const visitors=Number(s.visitors)||0;const orders=Number(s.orders_sent)||0;const conv=visitors?orders/visitors*100:0;const prevVisitors=Number(s.visitors_prev)||0;const prevConv=prevVisitors?(Number(s.orders_sent_prev)||0)/prevVisitors*100:0;grid.innerHTML=[statTile2("Visitors",visitors.toLocaleString(),pctChange2(visitors,s.visitors_prev),spark),statTile2("Unique visitors",(Number(s.unique_visitors)||0).toLocaleString(),pctChange2(s.unique_visitors,s.unique_visitors_prev),spark),statTile2("Members",(Number(s.members)||0).toLocaleString(),{dir:Number(s.members_new)>0?"up":"flat",text:Number(s.members_new)>0?`+${s.members_new} this month`:"no change"},null),statTile2("Requests started",(Number(s.requests_started)||0).toLocaleString(),pctChange2(s.requests_started,s.requests_started_prev),null),statTile2("Orders sent",orders.toLocaleString(),pctChange2(orders,s.orders_sent_prev),null),statTile2("Conversion rate",conv.toFixed(1)+"%",pctChange2(conv,prevConv),null)].join("");if(stamp){const since=s.first_view_at?` \xB7 collecting since ${formatDay(s.first_view_at)}`:"";stamp.innerHTML=`<span class="stats-live-dot"></span>Live from the site \u2014 read just now${escapeHtml(since)}. ${escapeHtml((Number(s.page_views)||0).toLocaleString())} page views in the window.`}},renderRankList2=function(){const list=document.getElementById("rankList");if(!list)return;if(!siteStats){list.innerHTML=`<p class="dev-empty">No data yet.</p>`;return}const byService=siteStats.by_service||[];let rows=byService;let basis="from orders placed in the last 90 days";if(rows.length<2){rows=(siteStats.by_page||[]).map(r=>({label:prettyServicePath2(r.label),n:r.n}));basis="from service-page visits \u2014 switches to real orders once a few come in"}if(!rows.length){list.innerHTML=`<p class="dev-empty">Nothing to show yet \u2014 this fills in as people visit the service pages.</p>`;return}const total=rows.reduce((sum,r)=>sum+(Number(r.n)||0),0)||1;list.innerHTML=rows.slice(0,6).map(r=>{const pct=Math.round((Number(r.n)||0)*100/total);return`<div class="rank-row">
              <span class="rank-label">${escapeHtml(r.label)}</span>
              <div class="rank-bar-track"><div class="rank-bar-fill" style="width: ${pct}%;"></div></div>
              <span class="rank-value">${pct}%</span>
            </div>`}).join("")+`<p class="stats-stamp">${escapeHtml(basis)}.</p>`},prettyServicePath2=function(path){const file=String(path||"").split("/").pop().replace(/\.html$/,"");const names={business:"Business Websites",gaming:"Gaming Websites",seo:"SEO",hosting:"Website Hosting",updates:"Update Management",social:"Social Platforms"};return names[file]||file||"Other"},perfTile2=function(label,value,state){const icon=state==="warning"?WARN:TICK;return`<div class="perf-tile">
              <span class="perf-icon ${state}">${icon}</span>
              <div class="perf-body">
                <span class="perf-label">${escapeHtml(label)}</span>
                <span class="perf-value">${escapeHtml(value)}</span>
              </div>
            </div>`},renderPerfGrid2=function(){const grid=document.getElementById("perfGrid");const stamp=document.getElementById("healthStamp");if(!grid)return;const s=siteStats||{};const snap=statsSnapshot||{};const tiles=[];if(s.avg_load_ms!=null){const sec=Number(s.avg_load_ms)/1e3;tiles.push(perfTile2("Avg. page load",sec.toFixed(1)+"s",sec<=2.5?"good":"warning"))}else{tiles.push(perfTile2("Avg. page load","\u2014","good"))}const up=snap.uptime||{};if(up.status){tiles.push(perfTile2("Uptime",up.status==="up"?"Up":"Down",up.status==="up"?"good":"warning"))}if(s.bounce_pct!=null){const b=Number(s.bounce_pct);tiles.push(perfTile2("Bounce rate",b.toFixed(0)+"%",b<=60?"good":"warning"))}else{tiles.push(perfTile2("Bounce rate","\u2014","good"))}if(s.avg_session_sec!=null){const sec=Number(s.avg_session_sec);const m=Math.floor(sec/60);tiles.push(perfTile2("Avg. session",(m?m+"m ":"")+Math.round(sec%60)+"s","good"))}else{tiles.push(perfTile2("Avg. session","\u2014","good"))}if(s.open_chats!=null){const open=Number(s.open_chats);tiles.push(perfTile2("Open chats",String(open),open>0?"warning":"good"))}const ps=snap.pagespeed||{};if(ps.performance!=null){tiles.push(perfTile2("PageSpeed",String(ps.performance),Number(ps.performance)>=90?"good":"warning"))}grid.innerHTML=tiles.join("");if(stamp){const when=snap.generatedAt?formatDay(snap.generatedAt):null;const by=snap.checkedBy?` by ${snap.checkedBy}`:"";stamp.textContent=when?`Page load, bounce, session and chats are live. Uptime and PageSpeed come from the last daily check \u2014 ${when}${by}.`:`Page load, bounce, session and chats are live. No daily check has run yet.`}};var tierTagsHtml=tierTagsHtml2,scoreClass=scoreClass2,domainAndHealthHtml=domainAndHealthHtml2,orderKindBadge=orderKindBadge2,canRemoveProjects=canRemoveProjects2,projectActionsHtml=projectActionsHtml2,wireDomainAndHealthHandlers=wireDomainAndHealthHandlers2,renderPendingPanel=renderPendingPanel2,wireProjectActions=wireProjectActions2,renderAssignedPanel=renderAssignedPanel2,renderFinishedPanel=renderFinishedPanel2,permissionCheckboxes=permissionCheckboxes2,canManageDevelopers=canManageDevelopers2,renderDevelopersPanel=renderDevelopersPanel2,chatDisplayName=chatDisplayName2,renderChatPanel=renderChatPanel2,totalVisitorUnread=totalVisitorUnread2,canDeleteVisitorChats=canDeleteVisitorChats2,renderVisitorChat=renderVisitorChat2,subscribeToVisitorChat=subscribeToVisitorChat2,canManageDiscounts=canManageDiscounts2,codeStatus=codeStatus2,activeForLabel=activeForLabel2,canManageConcepts=canManageConcepts2,conceptRowHtml=conceptRowHtml2,conceptFormHtml=conceptFormHtml2,renderConceptsPanel=renderConceptsPanel2,renderAllPanels=renderAllPanels2,waitForAuthReady=waitForAuthReady2,renderClientPreview=renderClientPreview2,firstNameOf=firstNameOf2,resolvePortalFirstName=resolvePortalFirstName2,applyPortalRole=applyPortalRole2,scriptImageUrl=scriptImageUrl2,canManageScripts=canManageScripts2,scriptSize=scriptSize2,slugify=slugify2,currentDelivery=currentDelivery2,scriptFormHtml=scriptFormHtml2,renderScriptsPanel=renderScriptsPanel2,minutesSince=minutesSince2,ageLabel=ageLabel2,isStuck=isStuck2,orderItemsSummary=orderItemsSummary2,orderRowHtml=orderRowHtml2,renderOrdersPanel=renderOrdersPanel2,scriptSaleRowHtml=scriptSaleRowHtml2,renderScriptSalesPanel=renderScriptSalesPanel2,canPriceQuotes=canPriceQuotes2,money=money2,invoiceDate=invoiceDate2,toCents=toCents2,invoicePayUrl=invoicePayUrl2,blankDraft=blankDraft2,draftFromInvoice=draftFromInvoice2,lineWillSave=lineWillSave2,draftTotals=draftTotals2,invoiceLineLabel=invoiceLineLabel2,invoiceFormHtml=invoiceFormHtml2,invoiceStatusTag=invoiceStatusTag2,invoiceRowHtml=invoiceRowHtml2,renderInvoicesPanel=renderInvoicesPanel2,readDraftFromForm=readDraftFromForm2,refreshDraftTotals=refreshDraftTotals2,wireInvoicePanel=wireInvoicePanel2,invoiceEmail=invoiceEmail2,invoiceMailto=invoiceMailto2,clientInvoicesHtml=clientInvoicesHtml2,pctChange=pctChange2,sparkPoints=sparkPoints2,sparkSvg=sparkSvg2,statTile=statTile2,renderStatGrid=renderStatGrid2,renderRankList=renderRankList2,prettyServicePath=prettyServicePath2,perfTile=perfTile2,renderPerfGrid=renderPerfGrid2;let chatListMode="clients";let activeChatId=null;devTabs.forEach(tab=>{tab.addEventListener("click",()=>{devTabs.forEach(t=>t.classList.remove("active"));tab.classList.add("active");document.querySelectorAll(".dev-tab-panel").forEach(p=>{p.hidden=p.dataset.panel!==tab.dataset.tab})})});async function cancelProject(id){const why=window.prompt("Cancel this project? It stays on record, marked cancelled.\n\nReason (optional):","");if(why===null)return;const{error}=await digicodeSupabase.rpc("cancel_project",{p_id:id,p_reason:why});if(error){window.alert("Couldn't cancel that \u2014 if this is the first time, run supabase/026_project_cancel_delete.sql.");return}await loadProjects();renderAllPanels2()}async function deleteProject(id,label,kind){const extra=kind==="paid"?"\n\nThis one was PAID. Deleting it removes the record of that payment from the portal \u2014 cancel it instead unless this is a test row.":"";if(!window.confirm(`Delete "${label}" permanently? This can't be undone.${extra}`))return;const{error}=await digicodeSupabase.from("projects").delete().eq("id",id);if(error){window.alert("Couldn't delete that \u2014 your account may not have permission.");return}await loadProjects();renderAllPanels2()}async function restoreProject(id){const{error}=await digicodeSupabase.rpc("restore_project",{p_id:id});if(error){window.alert("Couldn't restore that project.");return}await loadProjects();renderAllPanels2()}async function updateProjectFields(id,fields){await digicodeSupabase.from("projects").update(fields).eq("id",id);await loadProjects()}async function checkWebsiteHealth(projectId){const project=getProjects().find(p=>p.id===projectId);if(!project||!project.domain)return;const key=getPsiKey();if(!key){await updateProjectFields(projectId,{health:{error:"Add a free Google PageSpeed API key in the Developers tab to run live checks."}});renderAllPanels2();return}await updateProjectFields(projectId,{health:{loading:true}});renderAllPanels2();const url=`https://www.googleapis.com/pagespeedonline/v5/runPagespeed?url=${encodeURIComponent(project.domain)}&category=performance&category=seo&category=accessibility&category=best-practices&strategy=mobile&key=${encodeURIComponent(key)}`;try{const res=await fetch(url);const data=await res.json();if(!res.ok)throw new Error(data?.error?.message||`Request failed (${res.status})`);const cats=data.lighthouseResult?.categories||{};const audits=data.lighthouseResult?.audits||{};await updateProjectFields(projectId,{health:{performance:cats.performance?Math.round(cats.performance.score*100):null,seo:cats.seo?Math.round(cats.seo.score*100):null,accessibility:cats.accessibility?Math.round(cats.accessibility.score*100):null,bestPractices:cats["best-practices"]?Math.round(cats["best-practices"].score*100):null,lcp:audits["largest-contentful-paint"]?.displayValue||"\u2014",cls:audits["cumulative-layout-shift"]?.displayValue||"\u2014",fcp:audits["first-contentful-paint"]?.displayValue||"\u2014",checkedAt:new Date().toISOString()}})}catch(err){await updateProjectFields(projectId,{health:{error:err.message||"Couldn't fetch results \u2014 check the domain and try again."}})}renderAllPanels2()}async function assignProject(id){const project=getProjects().find(p=>p.id===id);if(!project)return;await updateProjectFields(id,{status:"assigned",assigned_dev:window.digicodePortalEmail||"Dev",assigned_at:new Date().toISOString()});if(project.clientEmail){const subject=encodeURIComponent(`Your ${project.service} project is underway`);const body=encodeURIComponent(`Hi ${project.clientName||"there"},

A developer has been assigned to your project and will be in touch with you soon via the contact details you provided.

Thanks,
DigiCode`);window.location.href=`mailto:${project.clientEmail}?subject=${subject}&body=${body}`}renderAllPanels2()}async function addProgressNote(id,text){const project=getProjects().find(p=>p.id===id);if(!project)return;const notes=[...project.progressNotes,{date:new Date().toISOString(),note:text}];await updateProjectFields(id,{progress_notes:notes});renderAssignedPanel2()}async function finishProject(id){await updateProjectFields(id,{status:"finished",finished_at:new Date().toISOString()});renderAllPanels2()}const DEV_PERMISSIONS=[["View Pending","View Pending Projects"],["View Assigned","View Assigned Projects"],["View Finished","View Finished Projects"],["Assign Projects","Assign Projects to Self"],["Manage Developers","Manage Other Developers"],["Delete Live Chats","Delete Live Chat History"],["Manage Discount Codes","Manage Discount Codes"],["Delete Projects","Delete Projects"],["Manage Scripts","List Scripts For Sale"],["Price Quotes","Price & Send Quotes"]];const DEV_RANKS=["Junior Developer","Developer","Senior Developer","Lead Developer"];const DEFAULT_NEW_DEV_PERMISSIONS=["View Pending","View Assigned","View Finished"];let editingDevId=null;async function deleteVisitorConversation(id,label){if(!window.confirm(`Delete the whole conversation with ${label}? This can't be undone.`))return;const{error}=await digicodeSupabase.from("visitor_chats").delete().eq("id",id);if(error){window.alert("Couldn't delete that conversation \u2014 your account may not have permission.");return}if(activeChatId==="visitor:"+id)activeChatId=null;await loadVisitorChats();renderChatPanel2()}async function deleteReadVisitorConversations(){const stale=getVisitorChats().filter(c=>visitorUnreadCount(c.id)===0);if(!stale.length){window.alert("Nothing to clear \u2014 every conversation still has unread messages.");return}if(!window.confirm(`Delete ${stale.length} read conversation${stale.length===1?"":"s"}? This can't be undone.`))return;const{error}=await digicodeSupabase.from("visitor_chats").delete().in("id",stale.map(c=>c.id));if(error){window.alert("Couldn't clear those \u2014 your account may not have permission.");return}activeChatId=null;await loadVisitorChats();renderChatPanel2()}async function renderDiscountsPanel(){const panel=document.getElementById("discountsPanel");if(!panel)return;const{data,error}=await digicodeSupabase.from("discount_codes").select("*").order("created_at",{ascending:false});if(error){panel.innerHTML=`<p class="dev-empty">Couldn't load discount codes. If this is the first time, run supabase/020_discount_codes.sql.</p>`;return}const codes=data||[];const canManage=canManageDiscounts2();const formHtml=canManage?`
      <form class="add-dev-form" id="addDiscountForm">
        <h3>Create a discount code</h3>
        <div class="form-grid">
          <div class="form-group">
            <label for="dcCode">Code</label>
            <input type="text" id="dcCode" placeholder="e.g. LAUNCH20" maxlength="40" required />
          </div>
          <div class="form-group">
            <label for="dcKind">Type</label>
            <select id="dcKind">
              <option value="percent">Percentage off</option>
              <option value="fixed">Fixed amount off</option>
            </select>
          </div>
          <div class="form-group">
            <label for="dcAmount">Amount</label>
            <input type="number" id="dcAmount" min="1" step="0.01" placeholder="20" required />
            <span class="form-hint" id="dcAmountHint">Percent off, 1\u2013100.</span>
          </div>
          <div class="form-group">
            <label for="dcApplies">Applies to</label>
            <select id="dcApplies">
              <option value="both">Everything in the cart</option>
              <option value="one_time">One-time builds only</option>
              <option value="subscription">Subscriptions only</option>
            </select>
          </div>
          <div class="form-group">
            <label for="dcDays">Runs for</label>
            <input type="number" id="dcDays" min="1" max="30" value="14" required />
            <span class="form-hint">Days from now. Maximum 30.</span>
          </div>
          <div class="form-group">
            <label for="dcMaxUses">Usage limit</label>
            <input type="number" id="dcMaxUses" min="1" placeholder="Leave blank for unlimited" />
          </div>
        </div>
        <p class="portal-login-error" id="dcError" hidden></p>
        <button type="submit" class="btn btn-primary">Create Code</button>
      </form>`:`<p class="dev-empty">You can see the codes here, but creating and changing them needs the Lead Developer rank or the "Manage Discount Codes" permission.</p>`;const listHtml=codes.length?codes.map(c=>{const st=codeStatus2(c);const value=c.kind==="percent"?`${+c.amount}% off`:`$${(+c.amount).toFixed(2)} off`;const scope=c.applies_to==="one_time"?"one-time only":c.applies_to==="subscription"?"subscriptions only":"everything";const uses=c.max_uses!=null?`${c.times_used} of ${c.max_uses} used`:`${c.times_used} used`;return`
          <div class="discount-row">
            <div class="discount-main">
              <span class="discount-code">${escapeHtml(c.code)}</span>
              <span class="discount-value">${value} \xB7 ${scope}</span>
              <span class="discount-meta">${activeForLabel2(c)} \xB7 ${uses} \xB7 expires ${formatDate(c.expires_at)}</span>
            </div>
            <span class="discount-status tone-${st.tone}">${st.label}</span>
            ${canManage?`<div class="discount-actions">
                     <button type="button" class="btn btn-ghost btn-small dc-toggle" data-id="${c.id}" data-active="${c.active}">${c.active?"Turn off":"Turn on"}</button>
                     <button type="button" class="cart-item-remove dc-delete" data-id="${c.id}" data-code="${escapeHtml(c.code)}" aria-label="Delete ${escapeHtml(c.code)}">&times;</button>
                   </div>`:""}
          </div>`}).join(""):`<p class="dev-empty">No discount codes yet.</p>`;panel.innerHTML=formHtml+`<div class="discount-list">${listHtml}</div>`;const kindEl=document.getElementById("dcKind");kindEl?.addEventListener("change",()=>{document.getElementById("dcAmountHint").textContent=kindEl.value==="percent"?"Percent off, 1\u2013100.":"Dollars off the total."});document.getElementById("addDiscountForm")?.addEventListener("submit",async e=>{e.preventDefault();const err=document.getElementById("dcError");const code=document.getElementById("dcCode").value.trim().toUpperCase();const kind=document.getElementById("dcKind").value;const amount=parseFloat(document.getElementById("dcAmount").value);const applies=document.getElementById("dcApplies").value;const days=parseInt(document.getElementById("dcDays").value,10);const maxUsesRaw=document.getElementById("dcMaxUses").value.trim();const fail=m=>{err.textContent=m;err.hidden=false};err.hidden=true;if(!/^[A-Z0-9_-]{3,40}$/.test(code))return fail("Codes can use letters, numbers, dashes and underscores \u2014 3 to 40 characters.");if(!(amount>0))return fail("Enter a discount amount above zero.");if(kind==="percent"&&amount>100)return fail("A percentage discount can't be more than 100%.");if(!(days>=1&&days<=30))return fail("A code can run for between 1 and 30 days.");const{error:insErr}=await digicodeSupabase.from("discount_codes").insert({code,kind,amount,applies_to:applies,expires_at:new Date(Date.now()+days*864e5).toISOString(),max_uses:maxUsesRaw?parseInt(maxUsesRaw,10):null,created_by:window.digicodePortalEmail||null});if(insErr){return fail(/duplicate|unique/i.test(insErr.message)?"That code already exists.":"Couldn't create that code \u2014 your account may not have permission.")}renderDiscountsPanel()});panel.querySelectorAll(".dc-toggle").forEach(btn=>{btn.addEventListener("click",async()=>{await digicodeSupabase.from("discount_codes").update({active:btn.dataset.active!=="true"}).eq("id",Number(btn.dataset.id));renderDiscountsPanel()})});panel.querySelectorAll(".dc-delete").forEach(btn=>{btn.addEventListener("click",async()=>{if(!window.confirm(`Delete the code ${btn.dataset.code}? This can't be undone.`))return;await digicodeSupabase.from("discount_codes").delete().eq("id",Number(btn.dataset.id));renderDiscountsPanel()})})}let conceptsCache=[];async function loadConcepts(){const{data,error}=await digicodeSupabase.from("concept_builds").select("*").order("created_at",{ascending:false});if(!error)conceptsCache=data||[];return conceptsCache}let conceptEditingId=null;async function saveConcept(e){e.preventDefault();const f=e.currentTarget;const btn=f.querySelector('button[type="submit"]');const original=btn.textContent;btn.disabled=true;btn.textContent="Saving\u2026";const{error}=await digicodeSupabase.rpc("save_concept_build",{p_id:f.dataset.id?Number(f.dataset.id):null,p_name:f.name.value,p_client:f.client.value,p_slug:f.slug.value,p_url:f.url.value,p_summary:f.summary.value,p_status:f.status.value});btn.disabled=false;btn.textContent=original;if(error){window.alert("Couldn't save that \u2014 the slug may already be taken, your account may not have permission, or supabase/040_concept_builds.sql hasn't been run yet.");return}conceptEditingId=null;await loadConcepts();renderConceptsPanel2()}async function setConceptStatus(id,to){const{error}=await digicodeSupabase.rpc("set_concept_status",{p_id:id,p_status:to});if(error){window.alert("Couldn't change that \u2014 your account may not have permission.");return}await loadConcepts();renderConceptsPanel2()}async function deleteConcept(id,name){const warning='Remove "'+name+`" from the concept register?

This deletes the entry here only. The build's folder stays in the repo and its URL keeps working until it is deleted there too.

This can't be undone.`;if(!window.confirm(warning))return;const{error}=await digicodeSupabase.rpc("delete_concept_build",{p_id:id});if(error){window.alert("Couldn't delete that \u2014 your account may not have permission.");return}await loadConcepts();renderConceptsPanel2()}const viewSwitchBtns=document.querySelectorAll(".view-switch-btn");const viewSwitchRow=document.querySelector(".view-switch-row");const devViewContainer=document.getElementById("devViewContainer");const clientViewContainer=document.getElementById("clientViewContainer");const portalWelcomeName=document.getElementById("portalWelcomeName");viewSwitchBtns.forEach(btn=>{btn.addEventListener("click",()=>{viewSwitchBtns.forEach(b=>b.classList.remove("active"));btn.classList.add("active");const isClient=btn.dataset.view==="client";if(devViewContainer)devViewContainer.hidden=isClient;if(clientViewContainer)clientViewContainer.hidden=!isClient;if(isClient)renderClientPreview2()})});const SCRIPT_BUCKET="script-files";const SCRIPT_IMAGE_BUCKET="script-images";let scriptsCache=[];let editingScriptId=null;async function loadScripts(){const{data,error}=await digicodeSupabase.from("script_products").select("*").order("created_at",{ascending:false});if(!error)scriptsCache=data||[];return scriptsCache}const SCRIPT_CATEGORIES=["Cars","Custom Build Scripts","CFX Scripts","MLOs"];const CFX_CATEGORIES=["CFX Scripts","MLOs"];async function saveScript(existing){const status=document.getElementById("sfStatus");const save=document.getElementById("sfSave");const name=document.getElementById("sfName").value.trim();const priceRaw=document.getElementById("sfPrice").value.replace(/[^0-9.]/g,"");const cents=Math.round(parseFloat(priceRaw||"0")*100);const file=document.getElementById("sfFile").files[0];if(!name)return;if(!Number.isFinite(cents)||cents<0){status.textContent="That price doesn't look right.";return}const category=document.getElementById("sfCategory")?.value||"Custom Build Scripts";const cfxDetails=(document.getElementById("sfCfxDetails")?.value||"").trim();const delivery=document.querySelector('input[name="sfDelivery"]:checked')?.value||"file";const driveUrl=(document.getElementById("sfDrive")?.value||"").trim();if(delivery==="cfx"){if(!cfxDetails){status.textContent="Note which cfx.re asset this is \u2014 you'll need it to complete the transfer once it sells.";return}}else if(delivery==="drive"){if(!/^https:\/\/(drive|docs)\.google\.com\//i.test(driveUrl)){status.textContent="That doesn't look like a Google Drive link \u2014 it should start with https://drive.google.com/";return}}else if(!existing&&!file){status.textContent="Choose the script file to upload.";return}else if(existing&&!existing.file_path&&!existing.drive_url&&!file){status.textContent="This listing has no file yet \u2014 choose one to upload.";return}const MAX_UPLOAD=50*1024*1024;if(delivery==="file"&&file&&file.size>MAX_UPLOAD){status.textContent=`That file is ${(file.size/1048576).toFixed(1)}MB. The plan caps uploads at 50MB \u2014 split it, or switch to a Google Drive link above.`;return}const imageCheck=document.getElementById("sfImage").files[0];if(imageCheck&&imageCheck.size>5*1024*1024){status.textContent="That image is over 5MB \u2014 use a smaller one.";return}save.disabled=true;status.textContent=file?"Uploading\u2026":"Saving\u2026";const fields={name,slug:existing?existing.slug:slugify2(name)+"-"+Date.now().toString(36).slice(-4),platform:document.getElementById("sfPlatform").value.trim()||null,version:document.getElementById("sfVersion").value.trim()||null,summary:document.getElementById("sfSummary").value.trim()||null,description:document.getElementById("sfDesc").value.trim()||null,price_cents:cents,category,cfx_details:delivery==="cfx"?cfxDetails:null,active:document.getElementById("sfActive").checked,updated_at:new Date().toISOString()};try{if(delivery==="cfx"){fields.drive_url=null;fields.file_path=null;fields.file_name=null;fields.file_bytes=null}else if(delivery==="drive"){fields.drive_url=driveUrl;fields.file_path=null;fields.file_name=null;fields.file_bytes=null}else{fields.drive_url=null}if(delivery==="file"&&file){const path=fields.slug+"/"+Date.now()+"-"+file.name.replace(/[^A-Za-z0-9._-]/g,"_");const up=await digicodeSupabase.storage.from(SCRIPT_BUCKET).upload(path,file,{upsert:false});if(up.error)throw up.error;fields.file_path=path;fields.file_name=file.name;fields.file_bytes=file.size}const image=document.getElementById("sfImage").files[0];if(image){status.textContent="Uploading image\u2026";const ipath=fields.slug+"/"+Date.now()+"-"+image.name.replace(/[^A-Za-z0-9._-]/g,"_");const iup=await digicodeSupabase.storage.from(SCRIPT_IMAGE_BUCKET).upload(ipath,image,{upsert:false,contentType:image.type||void 0});if(iup.error)throw iup.error;if(existing&&existing.image_path){await digicodeSupabase.storage.from(SCRIPT_IMAGE_BUCKET).remove([existing.image_path]).catch(()=>{})}fields.image_path=ipath}if(existing){const{error}=await digicodeSupabase.from("script_products").update(fields).eq("id",existing.id);if(error)throw error}else{fields.created_by=window.digicodePortalEmail||null;const{error}=await digicodeSupabase.from("script_products").insert(fields);if(error)throw error}editingScriptId=null;await loadScripts();renderScriptsPanel2()}catch(err){save.disabled=false;status.textContent="Couldn't save that \u2014 "+(err&&err.message?err.message:"check permissions.")}}async function clearScriptImage(s){if(!s||!s.image_path)return;if(!window.confirm("Remove the thumbnail from this listing?"))return;try{await digicodeSupabase.storage.from(SCRIPT_IMAGE_BUCKET).remove([s.image_path]);const{error}=await digicodeSupabase.from("script_products").update({image_path:null,updated_at:new Date().toISOString()}).eq("id",s.id);if(error)throw error}catch(e){window.alert("Couldn't remove that image.");return}await loadScripts();renderScriptsPanel2()}async function toggleScript(id){const s=scriptsCache.find(x=>String(x.id)===String(id));if(!s)return;const hasCfxNote=(s.cfx_details||"").trim();if(!s.file_path&&!s.drive_url&&!hasCfxNote&&!s.active){window.alert("Add a script file, a Google Drive link, or which cfx.re asset this is before listing it for sale.");return}const{error}=await digicodeSupabase.from("script_products").update({active:!s.active,updated_at:new Date().toISOString()}).eq("id",id);if(error){window.alert("Couldn't change that \u2014 your account may not have permission.");return}await loadScripts();renderScriptsPanel2()}async function deleteScript(id){const s=scriptsCache.find(x=>String(x.id)===String(id));if(!s)return;if(s.sales_count>0){window.alert(`"${s.name}" has ${s.sales_count} sale${s.sales_count>1?"s":""}. Deleting it would break those buyers' downloads \u2014 unlist it instead.`);return}if(!window.confirm(`Delete "${s.name}" and its file? This can't be undone.`))return;try{if(s.file_path){await digicodeSupabase.storage.from(SCRIPT_BUCKET).remove([s.file_path])}const{error}=await digicodeSupabase.from("script_products").delete().eq("id",id);if(error)throw error}catch(err){window.alert("Couldn't delete that \u2014 your account may not have permission.");return}await loadScripts();renderScriptsPanel2()}const CONFIRM_ORDER_FN="https://phlmnlildwkkichlbtoy.supabase.co/functions/v1/confirm-order";const STUCK_MINUTES=10;let ordersCache=[];async function loadOrders(){const{data,error}=await digicodeSupabase.from("orders").select("*").order("created_at",{ascending:false}).limit(100);if(!error)ordersCache=data||[];return ordersCache}async function deleteOrderFromPortal(btn){const ref=btn.dataset.ref;const paid=!!btn.dataset.paid;const label=btn.dataset.label||"this order";const warning=paid?`Delete the PAID order for "${label}"?

This is a real payment. Removing it deletes the portal's record of it, and if the customer bought a script it also deletes the purchase their download link is issued against \u2014 they will not be able to recover it.

Stripe keeps its own record either way. This can't be undone.`:`Delete the unpaid order for "${label}"?

This can't be undone.`;if(!window.confirm(warning))return;btn.disabled=true;const{data,error}=await digicodeSupabase.rpc("delete_order",{p_reference:ref,p_force:paid});if(error){btn.disabled=false;window.alert("Couldn't delete that order \u2014 your account may not have permission, or supabase/039_delete_orders.sql hasn't been run yet.");return}if(data&&data.purchases_removed>0){window.alert("Order deleted, along with "+data.purchases_removed+(data.purchases_removed===1?" script purchase":" script purchases")+" that belonged to it.")}await Promise.all([loadOrders(),loadScriptSales()]);renderOrdersPanel2();renderScriptSalesPanel2()}async function confirmOrderFromPortal(btn){const ref=btn.dataset.ref;const original=btn.textContent;btn.disabled=true;btn.textContent="Checking\u2026";let paid=false;try{const res=await fetch(CONFIRM_ORDER_FN,{method:"POST",headers:{"Content-Type":"application/json",apikey:SUPABASE_ANON_KEY,Authorization:"Bearer "+SUPABASE_ANON_KEY},body:JSON.stringify({reference:ref})});const data=await res.json().catch(()=>({}));paid=!!data.paid}catch(e){}if(!paid){btn.disabled=false;btn.textContent=original;window.alert("Stripe doesn't show a completed payment for that order.\n\nThat usually means the customer never finished checkout \u2014 which is normal, and nothing needs doing. If you can see the payment in Stripe yourself, send me the reference and it can be settled by hand.");return}await Promise.all([loadOrders(),loadScriptSales(),loadProjects()]);renderOrdersPanel2();renderScriptSalesPanel2();renderPendingPanel2()}let scriptSalesCache=[];async function loadScriptSales(){const{data,error}=await digicodeSupabase.rpc("list_script_sales");if(!error)scriptSalesCache=data||[];return scriptSalesCache}async function markScriptSaleFulfilled(purchaseId){const{error}=await digicodeSupabase.rpc("mark_script_sale_fulfilled",{p_purchase_id:Number(purchaseId)});if(error){window.alert(error.message||"Couldn't update that \u2014 your account may not have permission.");return}await loadScriptSales();renderScriptSalesPanel2()}let invoicesCache=[];let invoiceDraft=null;async function loadInvoices(){const{data,error}=await digicodeSupabase.from("invoices").select("*").order("created_at",{ascending:false});if(!error)invoicesCache=data||[];return invoicesCache}window.digicodeInvoiceEmail=inv=>invoiceEmail2(inv);async function saveInvoice(){const d=readDraftFromForm2();if(!d)return;const err=document.getElementById("ivError");const btn=document.getElementById("ivSave");const show=msg=>{if(err){err.textContent=msg;err.hidden=false}};if(err)err.hidden=true;const orphan=d.lines.findIndex(l=>!lineWillSave2(l)&&toCents2(l.dollars)>0);if(orphan>-1){show("Line "+(orphan+1)+" has a price but no description, so it won't be saved. Give it a description, or clear the price.");return}const lines=d.lines.filter(lineWillSave2).map(l=>({description:l.description.trim(),qty:Number(l.qty)||1,unit_cents:toCents2(l.dollars)}));if(!lines.length){show("Add at least one line with a description.");return}if(btn){btn.disabled=true;btn.textContent="Saving\u2026"}const{data,error}=await digicodeSupabase.rpc("save_invoice",{p_id:d.id,p_project_id:d.projectId,p_client_name:d.clientName,p_client_email:d.clientEmail,p_title:d.title,p_lines:lines,p_notes:d.notes,p_due_at:d.dueAt||null,p_discount_cents:toCents2(d.discount)});if(btn){btn.disabled=false;btn.textContent=d.id?"Save changes":"Save draft"}if(error){show(error.message||"Couldn't save that invoice.");return}invoiceDraft=null;await loadInvoices();renderInvoicesPanel2();return data}async function sendInvoice(id){const inv=invoicesCache.find(i=>String(i.id)===String(id));if(!inv)return;const hasEmail=!!(inv.client_email||"").trim();let delivery="portal";if(hasEmail){const byEmail=window.confirm('Send "'+inv.title+'" for '+money2(inv.total_cents)+" to "+inv.client_email+"?\n\nOK \u2014 open your mail app with the quote and pay link written out, and put it in their portal too.\nCancel \u2014 put it in their client portal only.");delivery=byEmail?"both":"portal"}else if(!window.confirm("There's no email address on this invoice, so it can only go to the client portal. Send it there?")){return}const{error}=await digicodeSupabase.rpc("send_invoice",{p_id:inv.id,p_delivery:delivery});if(error){window.alert(error.message||"Couldn't send that invoice.");return}if(delivery==="both"){window.location.href=invoiceMailto2(inv)}await loadInvoices();renderInvoicesPanel2()}async function voidInvoice(id){const inv=invoicesCache.find(i=>String(i.id)===String(id));if(!inv)return;if(!window.confirm('Cancel "'+inv.title+'"? The pay link stops working straight away.')){return}const{error}=await digicodeSupabase.rpc("void_invoice",{p_id:inv.id});if(error){window.alert(error.message||"Couldn't cancel that invoice.");return}await loadInvoices();renderInvoicesPanel2()}async function deleteInvoice(id){const inv=invoicesCache.find(i=>String(i.id)===String(id));if(!inv)return;if(!window.confirm('Delete "'+inv.title+`" completely? This can't be undone.`))return;const{error}=await digicodeSupabase.from("invoices").delete().eq("id",inv.id);if(error){window.alert("Couldn't delete that \u2014 your account may not have permission.");return}await loadInvoices();renderInvoicesPanel2()}let siteStats=null;let statsSnapshot=null;async function loadSiteStats(){try{const{data,error}=await digicodeSupabase.rpc("site_stats");if(error)throw error;siteStats=data||null}catch(e){siteStats=null}return siteStats}async function loadStatsSnapshot(){try{const res=await fetch("data/site-stats.json?t="+Date.now(),{cache:"no-store"});statsSnapshot=res.ok?await res.json():null}catch(e){statsSnapshot=null}return statsSnapshot}const ARROW_UP=`<svg width="10" height="10" viewBox="0 0 10 10" fill="none"><path d="M5 8V2M5 2L2 5M5 2l3 3" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"/></svg>`;const ARROW_DOWN=`<svg width="10" height="10" viewBox="0 0 10 10" fill="none"><path d="M5 2v6M5 8L2 5M5 8l3-3" stroke="currentColor" stroke-width="1.4" stroke-linecap="round" stroke-linejoin="round"/></svg>`;const TICK=`<svg viewBox="0 0 24 24" fill="none"><path d="M5 12l4 4L19 6" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round"/></svg>`;const WARN=`<svg viewBox="0 0 24 24" fill="none"><path d="M12 9v4m0 4h.01M10.3 4.3L2.9 17a1.8 1.8 0 001.6 2.7h15a1.8 1.8 0 001.6-2.7L13.7 4.3a1.8 1.8 0 00-3.4 0z" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round"/></svg>`;async function refreshDashboard(){const btn=document.getElementById("statsRefresh");if(btn){btn.disabled=true;btn.textContent="Refreshing\u2026"}await Promise.all([loadSiteStats(),loadStatsSnapshot()]);renderStatGrid2();renderRankList2();renderPerfGrid2();if(btn){btn.disabled=false;btn.textContent="Refresh"}}const statsRefreshBtn=document.getElementById("statsRefresh");if(statsRefreshBtn)statsRefreshBtn.addEventListener("click",refreshDashboard);(async()=>{await Promise.all([waitForAuthReady2(),loadProjects(),loadTeam(),loadChats(),loadVisitorChats(),loadSiteStats(),loadStatsSnapshot(),loadScripts(),loadInvoices(),loadScriptSales(),loadOrders(),loadConcepts()]);renderAllPanels2();applyPortalRole2();subscribeToVisitorChat2()})()}const mgmtForm=document.getElementById("mgmtForm");if(mgmtForm){let currentCycle2=function(){const active=document.querySelector(".billing-tab.active");return active&&active.dataset.billing==="weekly"?"weekly":"monthly"},priceFor2=function(tierName,cycle){const card=[...document.querySelectorAll(".tier-card")].find(c=>c.querySelector(".tier-name")?.textContent.trim()===tierName);const el=card?.querySelector(".price-amount");if(!el)return{amount:"",period:""};return{amount:cycle==="weekly"?el.dataset.weekly:el.dataset.monthly,period:cycle==="weekly"?"/wk":"/mo"}},showForm2=function(tierName){const cycle=currentCycle2();const{amount,period}=priceFor2(tierName,cycle);tierInput.value=`${tierName} (${amount}${period})`;chosenLine.textContent=`${tierName} \u2014 ${amount}${period}, billed ${cycle}. Everything below helps us take the site over cleanly.`;mgmtForm.hidden=false;if(tierGrid)tierGrid.hidden=true;if(buildCta)buildCta.hidden=true;mgmtForm.scrollIntoView({behavior:"smooth",block:"start"})};var currentCycle=currentCycle2,priceFor=priceFor2,showForm=showForm2;const chosenLine=document.getElementById("mgmtChosenLine");const tierInput=document.getElementById("mgmtTierInput");const tierGrid=document.getElementById("tierGrid");const buildCta=document.getElementById("mgmtBuildCta");document.querySelectorAll(".mgmt-choose").forEach(btn=>{btn.addEventListener("click",()=>showForm2(btn.dataset.tier))});document.getElementById("mgmtChangeBtn")?.addEventListener("click",()=>{mgmtForm.hidden=true;if(tierGrid)tierGrid.hidden=false;if(buildCta)buildCta.hidden=false;tierGrid?.scrollIntoView({behavior:"smooth",block:"start"})});document.getElementById("mgmtCheckoutBtn")?.addEventListener("click",()=>{const tierName=(tierInput.value||"").split(" (")[0];if(!tierName)return;const cycle=currentCycle2();const{amount,period}=priceFor2(tierName,cycle);saveCartItems([{key:"Website Management::mgmtTier",service:"Website Management",type:"Management",name:tierName,price:amount,period}]);updateCartBadge();const lines=[];mgmtForm.querySelectorAll("input, textarea").forEach(el=>{const label=el.dataset.label;if(!label)return;if(el.type==="radio"||el.type==="checkbox"){if(el.checked)lines.push(label)}else{const val=el.value.trim();if(val)lines.push(`${label}: ${val}`)}});localStorage.setItem(CUSTOMER_KEY,JSON.stringify({service:"Website Management",lines}));window.location.href="cart.html"})}
